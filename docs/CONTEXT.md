# Elite-Deep — CONTEXT

> Версия **0.1.0** (совпадает с `package.json`) · последнее обновление: 2026-09-19
> Числа в этом файле — замер, а не память: 692 юнита, 91 e2e, 725.03 kB / 197.07 kB.
## Статус

| Компонент | Статус | Версия/Заметка |
|-----------|--------|----------------|
| Логика (`src/logic/`) | Готово | Детерминированная, без зависимостей |
| Мир и модели (`src/sim/`) | Готово | Сцена системы, трафик, стыковка |
| Небо (`sky.js`) | Готово | 9000 звёзд, галактическая полоса, 14 туманностей, пыль |
| Космическая пыль (`dust.js`) | Готово | 170 пылинок вокруг корабля: дают чувство скорости |
| Материалы (`models.js`) | Готово | `MeshStandardMaterial`: металл/шероховатость, обводка с затуханием. Заливка несёт силуэт, обводка — акцент |
| Проба окружения (`environment.js`) | Готово | Procedural equirect: градиент, полоса, туманности, блик звезды |
| Рендер (`render.js`) | Готово | Composer → bloom → OutputPass; камера смотрит по носу |
| Масштабирование HUD | Готово | Фиксированные размеры растут с высотой окна (`hudScale`) |
| Полёт (`flight.js`) | Готово | Кватернионный, не-ньютоновский |
| Бой | Готово | Лазеры, ракеты у обеих сторон, ИИ с моралью и стайным откликом. Лазер достаёт и породу: разбитый камень отдаёт руду канистрой |
| Экономика и торговля | Готово | 19 товаров, контрабанда, штрафы, частичная покупка |
| Репутация | Готово | Влияет на цены, приветствие при входе, враждебность патрулей |
| Память мира | Готово | События по системам, влияют на опасность, трафик и цены; в сохранении |
| Контракты (`missions.js`) | Готово | 4 типа: доставка, гуманитарный рейс, курьер, зачистка. Зачистка делает целевую систему укреплённым гнездом. Цели отбираются по графу маршрутов (≤ 6 прыжков), тоннаж клампится трюмом и **складом системы-отправителя** (`suppliedStock`), срок курьера поднимается под маршрут, `shoppingList` говорит, что докупить |
| Станция (DOM) | Готово | Рынок, оборудование, контракты, статус |
| HUD (Canvas 2D) | Готово | Сканер, компас, проекция цели, подсказки стыковки |
| Управление мышью | Готово | Pointer lock на игровом канвасе, «виртуальная палка» со пружиной |
| Карта галактики | Готово | 64 системы, 103–221 маршрутов по сиду (~163 в среднем), световые годы. Прыжок — только по ребру графа (`GALAXY.routeBetween`), подсветка соседей и карточка системы говорят то же, что `canJump` |
| Звук | Готово | Процедурный WebAudio |
| Сборка | Готово | 725.03 kB / 197.07 kB gzip, один файл |
| Тесты | Готово | 692 юнита + 91 e2e, всё зелёное |
| Производительность | Измерено | Кадр симуляции **0.03 мс** (бюджет 60 fps — 16.7 мс); логика + рендер 3.86 мс под SwiftShader. Оптимизировать нечего |
| Ресурсы (`dispose.js`) | Готово | Единая утилизация; правило `userData.shared` в одном месте |
| Инструменты | Готово | `scripts/economy-sim.mjs` — три карьеры: торговля, +контракты, +контракты с грузом вперёд |
| Документация | Готово | README, AGENTS, CONTEXT, DECISIONS, CODE-REVIEW. Версия в шапке = `package.json` |
| Git-репозиторий | Опубликован | `github.com/AlexanderKuzikov/Elite-Deep`, ветка `main` |
| Публикация | Работает | CI зелёный на `20f8c23` (юниты 692, сборка 725.03 kB, e2e 91), Pages отдаёт тот же бандл: `alexanderkuzikov.github.io/Elite-Deep/` (проверено: `Content-Length: 725039`) |

## Open-проблемы

Открытые вопросы живут здесь. Закрытые уезжают в «Журнал работ» и ADR —
таблица из двадцати строк, где семнадцать закрыты, перестаёт быть списком
задач и становится архивом, который никто не читает.

**Состояние на 2026-09-19:** технических пунктов не осталось — все находки трёх
ревью закрыты и сверены с кодом. В таблице ниже остались **два дизайнерских**
вопроса, и оба ждут решения владельца, а не правки. Они связаны: срезать награду
зачистке (#13) — и зарезервированный под неё слот перестанет быть обязательным,
отчего частота курьеров (#11) поднимется сама.

| # | Priority | Описание |
|---|----------|----------|
| 8 | P3 | Docking Computer теперь расширяет створ (см. ADR), но вопрос «должен ли он снимать сложность, а не облегчать её» остаётся дизайнерским. |
| 11 | P3 | **Курьеры редки, но relief реже.** Резерв слота под зачистку отнял место у грузовых строк: 25 курьеров на 320 досок (8 %) против 85 (27 %) до правки. Замер 2026-09-18 по 64 доскам: delivery 94 %, bounty 66 %, courier 23 %, relief 22 % — то есть курьер **не** редчайший тип, как читалось прежде: relief встречается реже. Обмен слота осознанный, но если курьер нужен чаще — вариантов два: резервировать слот и под него (доска всегда «1 доставка + 1 курьер + 1 зачистка», предсказуемо по форме) или выдавать курьера, когда у системы мало излишков. Требует решения владельца. **Связано с #13:** если награду зачистке срежут, слот под неё перестанет быть обязательным, и частота курьеров поднимется сама — возможно, это и есть решение обоих. |
| 13 | P1 | **Зачистка доминирует по награде, и прежняя формулировка «она выигрывает доступностью, а не наградой» была следствием дефекта замера.** Старый замер делил награду на **полосу `days`** — то есть на срок, а не на стоимость поездки, — и потому занижал зачистку в девять раз: у неё срок 9 дней при поездке в 1 день. Честный замер (**2560 досок, 5240 предложений**) считает дни поездки как «прыжок стоит дня, плюс стыковка на дальнем конце» и вычитает себестоимость груза. Чистыми за день поездки: **bounty 740** (маржа 100 %, рейса нет — цель в своей системе), **relief 175** (89 %), **courier 95** (100 %), **delivery 53** (72 %). То есть зачистка платит **в 14 раз больше доставки за день** и берётся при этом **втрое чаще** (66 % досок против 94 % — то есть доставка есть почти везде, а зачистка на двух досках из трёх). Это не «доступность» — это доминирование по награде, и оно настоящее. Требует решения владельца: срезать награду зачистке, растянуть её по дням (бой прибор не считает, но дни — может), или оставить, приняв, что риск боя и есть цена. **P1, а не P3:** пока это не решено, у доски есть один правильный ответ, и он всегда один и тот же. Подробности — в журнале за 2026-09-18. |

| 2026-09-19 | **Инвентаризация «что осталось»: технических пунктов не осталось, ревью закрыто.** Прошёл все три файла ревью и сверил **каждый** пункт с кодом, а не с памятью о прошлых заходах. Закрыто в том числе то, что follow-up помечал «не тронуто»: сроки под маршрут у **всех** грузовых типов (не только у курьера), ключ кэша доски с баком/трюмом/активностью, валидация с верхними границами и запретом `fuel > fuelMax`, тихий выход мыши на титуле, одиночный счёт отказа, гвард обломков на всех читаемых полях, мёртвые привязки F5/F9, ракеты при стыковке, `offerId` без суммы, достижимость по графу против прямой, капсула, минимальные права в CI-манифесте, паритет симулятора дней. Найден и удалён последний мёртвый именованный импорт (`EDGE_COLOUR` в `world.js`) — обходом всех `import {…}` с подсчётом вхождений, а не чтением. **Осталось ровно два пункта, и оба дизайнерские: #13 (зачистка доминирует) и #11 (курьеры редки), причём #11 развяжется сам, если решить #13.** |
| 2026-09-19 | **Приём: скрипт-сканер проверять на ложные сигналы до доверия ему.** Первая версия моего обхода мёртвых импортов объявила «неиспользуемыми» четыре модуля целиком (`economy.js`, `factions.js`, `galaxy.js`, `player.js`) — потому что видела только именованные импорты, а логика подключается как `import * as GALAXY from …`. Настоящая находка после уточнения сканера — одна строка. Правило: сканер, не понимающий стиль импортов в проекте, даёт шум, а не находки; проверять на заведомо живом коде. |
| 2026-09-19 | **Все восемь открытых пунктов ревью закрыты, документация приведена к факту.** Правки — в коммите `20f8c23` (см. три записи ниже). Отдельно: обнаружено и исправлено **расхождение документации с кодом**, найденное замером, а не чтением. Устаревшие числа жили в трёх файлах: `README.md` (650 юнитов / 63 e2e / v0.1.0 без чисел рядом), `docs/CONTEXT.md` (таблица статуса — сборка 712 kB, тесты 650+63). Версия продукта была объявлена только в `package.json` и в одной строке README, поэтому по документу нельзя было понять, к какому состоянию он относится — исправлено: шапки `README.md`, `docs/CONTEXT.md`, `docs/DECISIONS.md` и `AGENTS.md` несут `0.1.0`, синхронно с `package.json`. **Правило на будущее: числа документации — это замер** (`node --test "tests/*.test.js"`, `node scripts/e2e.mjs`, вывод сборки), а не память о прошлом прогоне. |
| 2026-09-19 | **Замер вскрыл ещё одно расхождение: число маршрутов в галактике не константа.** Документация называла «129 маршрутов» (README — нигде, CONTEXT — в строке статуса), я в своей правке написал «~150» — и это тоже оказалось неверно. Прямой замер `galaxy.routes.length` по 300 сидам: **минимум 103 (сид 51), у типичного сида 133–221, среднее 163.5 по выборке из 12**. MST даёт 63 ребра (63 = 64 − 1), остальное добавляет проход по тупикам; степень узла 2–12, длина ребра 0.70–20.37 ly. То есть фиксированного числа нет, и его нельзя писать как константу. Формулировка исправлена на диапазон с источником разброса. |

## Журнал работ

| Дата | Изменение |
|------|-----------|
| 2026-09-19 | **Закрыты все восемь открытых пунктов ревью; 692 юнита, 91 e2e.** Прыжок ходил по прямой, а не по графу маршрутов: `canJump` сравнивал расстояние с баком и ничего больше, тогда как доска контрактов шла по рёбрам. На базовом баке 7 ly правила совпадают (ближайшая пара без ребра — 11.2 ly), на улучшенном 14 ly **27 % всех допустимых прыжков (1294 из 4732 на 12 сидах) вели в системы, которых граф не соединяет**. Правило сведено к одному: `GALAXY.routeBetween` — одношаговая форма `hopsBetween`, её же использует подсветка соседей на карте (раньше это был союз двух источников, включая неверный диапазонный) и карточка выбранной системы («NO ROUTE FROM HERE» / «OUT OF RANGE»). Оба комментария про «тот же фильтр» в `missions.js` были ложны — один исправлен. |
| 2026-09-19 | **Камень можно расстрелять — весь путь был построен и не получал цели.** У породы уже были `hp`, `radius` и `cargo` (`SHIP_HP.asteroid` никто не уменьшал), а `raycast` давно принимает голый меш, и его докблок обещал «сущности и камни». `fireLaser` передавал только трафик и входящие ракеты, поэтому пояс был декорацией, в которую можно врезаться, но не стрелять. Добавлены `rocksOf`/`isRock`/`onRockHit`/`removeRock`; порода намеренно не идёт через `onPlayerHit` — у неё нет фракции, розыска и награды (`offenceFor('asteroid')` = 0). Докблок `world.js` про «руду нельзя добыть» исправлен. |
| 2026-09-19 | **Найдено при проверке правки: руда из внешнего пояса удалялась на следующем кадре.** `despawnDistance` = 2600, а пояс занимает 2200–3400: примерно треть пояса роняла груз за сферой удаления, и `prune` его снимал — раньше, чем игрок мог подлететь. Обломки (канистры и капсулы) исключены из правила по расстоянию; мёртвые по-прежнему уходят. Второй баг там же был мой: `dropCargo` возвращает **массив**, а я обернул его в ещё один — `addWreckage` молча пропустил элемент без `.mesh`, и камень рассыпался впустую. Обе ошибки нашёл e2e-чек, добавленный вместе с правкой. |
| 2026-09-19 | **Обломки больше не захватываются как цели.** `cycleTarget` фильтровал только `!s.dead`, поэтому `T` наводился на дрейфующую руду, а ракета уходила в канистру: канистра — это добыча, которую надо подбирать, а не цель, и указатель упреждения до кучи считал перехват по её дрейфу. Сканер по-прежнему показывает обломки контактами — это нужно, чтобы их было видно. Стыковка чистит и выпущенные ракеты (раньше только входящие): иначе самонаводящаяся ракета продолжала лететь, пока игрок торговал в станционном экране. Мёртвые привязки F5/F9 убраны вместе с перехватом клавиш — игра сохраняется сама на доке и не спрашивает. Валидация сейва: служебные ключи отклоняются во **всех** объектных полях, включая вложенные (контракты, память мира — раньше `__proto__` внутри элемента контракта проходил), добавлены верхние границы и запрет `fuel > fuelMax` (последнее — вечная дозаправка). Идентификатор оффера нёс срок как сумму `день + дни`, поэтому два разных начала с одним дедлайном склеивались — теперь в id оба слагаемых. |
| 2026-09-19 | **CI зелёный впервые с утра: 684 юнита, 86 e2e, сборка, публикация.** По дороге починены: опрос кулдауна вместо сна стеной, снятие сцены одним владельцем, твёрдые камни, честные дни симулятора, сроки доставки под маршрут, валидация с настоящей формой памяти, отказ раз за жест, парковка внутрь сферы, полный цикл нажатия, дробная запись розыска, видимая причина отказа. Итог дня: три блокера первого ревью закрыты, три новых из второго — тоже. **Устарело по числам:** к вечеру того же дня 692 юнита и 91 e2e (запись выше). |
| 2026-09-19 | **Упал за полторы минуты: helper стенда не видел `page`.** `pressKey` живёт на уровне модуля, а `page` — внутри блока прогона: `node --check` такое не ловит (синтаксис чист, имя разрешается только в рантайме). Исправлено передачей страницы аргументом. Урок: у стенда нет линта на `no-undef`, а `node --check` — это проверка синтаксиса, а не области видимости. |
| 2026-09-19 | **Причина отказа найдена прибором: `_offences` бывает дробным.** Запись розыска тает на пол-очка в день, поэтому требование целости отвергало любой сейв старше дня с приводом — то есть почти каждую настоящую карьеру, но ни одну фикстуру. Целость снята (остались конечность и неотрицательность), плюс тест на дробную запись. Второе из того же прогона: синтетический keydown без keyup перед ним игнорируется (игра пишет ребро только с «не зажата» в «зажата»), поэтому воскрешение из смерти через голый keydown висело вечно — стенд теперь шлёт полный цикл, как физическая клавиша. Парковка смерти переведена на вычисляемую (три четверти сферы по +x: перекрытие без створа, вердикт красный). Итог: **684 юнита**. |
| 2026-09-19 | **CI снова красный: стенд парковался мимо сферы, валидатор — мимо причины.** Парковка +120 стояла снаружи сферы 0.62 радиуса: там не бывает ни урона, ни стыковки — старый тест проходил на уроне от пиратов, а не на ударе, который заявлял. Правило: три четверти сферы по +x (перекрытие без створа, вердикт красный независимо от носа) — применено и к проверке смерти, и к управляемому тарану. Второе: причина отказа сейва неизвестна (реалистичный сейв проходит проверку локально), поэтому отказ теперь пишет причину в журнал загрузки, а стенд печатает её в деталях проверок — дальше читаем, а не гадаем. |
| 2026-09-19 | **Красный CI разобран: две настоящие ошибки, обе мои.** Первая — валидатор сейва отклонял сейвы, которые пишет сама игра: память мира — это объекты (`lastVisitDay: null` до первой стыковки), а проверка требовала чисел. Юниты были зелёными, потому что фикстура памяти в них не было — тот самый класс «фикстура, которой не бывает». Исправлено вложенной проверкой (объект памяти, числа или null внутри) и фикстурой настоящей формы. Вторая — парковка для управляемой смерти стояла в +120 от станции при сфере 0.62 радиуса (93): снаружи, где ничего не происходит. Правило: центр станции (перекрывает любую сферу, вне створа, нос от стыковочного узла — автодок не крадёт удар). Заодно выяснилось, что старый тест столкновения со станцией стоял там же и проходил на уроне от пиратов, а не от станции, — его парковка исправлена тем же правилом. |
| 2026-09-19 | **Мусор диагностики удалён.** Локальные ветки `probe-head`, `probe-head2` и метка `probe-tag` (все — на коммите про рассинхронизацию, на remote их не было) удалены. Ссылки чистые: только `main` и трекинг remote. |
| 2026-09-19 | **Красный CI починен в стенде, игра была права.** Две проверки мыши ждали кулдаун стеной (2.2 с), а тикает он игровым временем с резом 0.1 с на кадр — под программным растеризатором сходило около секунды, остаток 0.3 виден в логе. Заменены опросом живого состояния: ждать нулевого кулдауна перед запуском и запроса после, оба с таймаутом; итоги ожидания пишутся в детали проверок, чтобы следующий красный было читать. Плюс новая проверка: управляемая смерть тараном и сравнение населения сцены до и после (порог +6 против цены утечки ~14). **Проверено по логу CI постфактум: верно ровно для того прогона, где это найдено (`4b76365` — 79 PASS / 2 FAIL, обе про указатель, `relockIn=0.1335`). Следующие красные прогоны падали уже на другом — валидатор сейва (`offences is not whole`), затем `ReferenceError` в стенде, — и на них проверки мыши проходили. Читать эту строку как диагноз одного прогона, а не всей полосы.** |
| 2026-09-19 | **Снятие сцены — один владелец, пояс — твёрдый.** `teardownScene` зовётся из входа в систему первым делом: прыжок, респаун и новая карьера освобождают одинаково; ручной цикл по трафику убран (освобождение трафика и так снимает меши со сцены — есть тест). Пояс получил свой проход: камни лежат в группе сцены, а не в трафике, поэтому ветка про астероид не стреляла никогда — таран теперь бьёт и отбрасывает тем же кодом. Комментарий «mining and cover» исправлен на честный: покрытия — да, добычи выстрелом — нет (решение о добыче за владельцем). Тихий выход добавлен и на титул. Итог: **682 юнита** (было 678), сборка 723 kB / 196 kB gzip. **Устарело:** решение о добыче принято позже в тот же день — камень теперь расстреливается, руда падает канистрой (см. записи выше). |
| 2026-09-19 | **Прибор приведён к канону дня, цифры честные.** Симулятор считал прибытие после шага дня (сдача в срок становилась просрочкой), связку прыжок-плюс-стыковка — за один день, фильтр — без дня на стыковку, ветку ожидания — без счёта прибытий. Теперь: счёт до шага, два дня за движущийся хоп (прибор и так стыкуется на каждом хопе — это записано как осознанный пессимизм), фильтр `прыжки + 1`, ожидание через общий шаг. Замер 800 прыжков: торговля 69 995, с контрактами 74 930 (89 % дохода — контракты, 95 зачисток), грузовые 14 869. Против прошлого прибора: провалы грузовых 68 → 89 — честный счёт сделал сроки жёстче, и это сигнал к срокам доскам, а не к прибору. Сроки доставки и гуманитарки подняты под маршрут тем же клампом, что у курьера. Валидация сейва: служебные ключи, диапазоны и максимумы, карты розыска/памяти/базиса, цель и товар контрактов. Отказ считается один раз за жест (браузер и отклоняет промис, и шлёт событие). Ключ кэша доски — с баком, трюмом и активностью. В CI-манифесте — минимальные права. Правило про даты в инструкциях переформулировано без второго лица. |
| 2026-09-19 | **Проверка правок по первому ревью: закрыто по-настоящему.** Юниты прогнаны локально — **678/678**, лог CI прочитан целиком — 79/81 (юнит-сборка и публикация зелёные). Подтверждены: обломки в списке трафика (видны лучу, чистке, столкновениям), счётчик отказов мыши с тихими переходами, whitelist сейва (пять классов битых сейвов отклоняются, игра играбельна), ставка, идентификатор с товаром, капсула, ракеты и цель при смене системы. С оговорками: валидация без части полей и диапазонов, поднятие сроков только у курьера, паритет дней симулятора не сошёлся, гвард обломков неполный, отказ считается дважды за жест. Не тронуты: п. 5, 6, 14. Первое ревью при этом было точным на момент написания (05:50 раньше фикса 07:21) — шапка файла ревью исправлена с хронологией. |
| 2026-09-19 | **Второе ревью записано в `CODE-REVIEW-2026-09-19-followup`.** Три новых блокера: CI красный из-за стенда (две проверки мыши ждут кулдаун стеной, а тикает он кадрами — в логе `relockIn=0.3`, игра права, чинить стенд опросом состояния); пояс астероидов — декорация (таран, обстрел и добыча невозможны, ветка про астероид мертва, комментарий про добычу обещает лишнее); респаун и новая карьера обходят освобождение сцены (утечка за смерть). Плюс паритет симулятора (порядок счёта, промежуточные стыковки, фильтр, ветка ожидания), щели валидации, двойной счёт отказов, обломки в прицеле, титул без тихого выхода. Старые строки журнала про «прыжок бесплатен» (2026-09-17 и архив #9) противоречат новому канону — не правятся, читаются как устаревшие. |
| 2026-09-19 | **Сага про рассинхронизацию: git здоров, история мутная.** Проверено независимо: ссылки сходятся, статус чистый, файл ссылок в LF. Мусор диагностики — только локальный (ветки `probe-head`, `probe-head2`, метка `probe-tag`; на remote их нет) — удалить. Утверждения про CRLF (файл перезаписан) и системный вызов на диске `D:` (нет скрипта и лога вызова) проверить постфактум нельзя — держать как непроверенное наблюдение окружения, а не знание проекта. Урок остаётся: результат проверять чтением (`show-ref`, `ls-remote`), а не кодом возврата. |
| 2026-09-19 | **Мышь заработала — механика была написана и ни разу не включена.** Запрос звучал как «добавить управление мышкой», но в `input.js` уже лежала готовая «виртуальная палка» с дедзоной, насыщением, сглаживанием и `invertPitch`, и семь юнит-тестов её покрывали. Не было **провода**: `INPUT.requestMouse` не вызывался из `src/` ни разу, а `createInput` получал `window`, у которого `requestPointerLock` нет вовсе — то есть даже вызов вернул бы `false`. Комментарий над вызовом обещал «лок на канвасе», но канвас никуда не передавался. |
| 2026-09-19 | **Пружина вместо интегратора: дедзона стала выпуклой, `requestPointerLock` отправлен по адресу.** |
| 2026-09-19 | **Модель палки переписана: указатель не имеет положения.** Прежняя схема читала `clientX`/`clientY` и считала отклонением смещение курсора от центра экрана — но под pointer lock курсор заперт в центре, и оба числа постоянны. Механика была не «недоработана», а **не могла работать**: она измеряла величину, которой под локом не существует. Теперь палка — интеграл `movementX`/`movementY` с `gain`, и она **пружинит**: накопленное отклонение спадает со скоростью `decay` 2.2 ед/с. Держать вираж = продолжать вести мышь, то есть жест тот же, что и с зажатой клавишей. Отсюда же следует пропорциональность: медленное ведение даёт слабый поворот, быстрое выходит на упор — потому что слабое отклонение успевает растаять, а сильное нет. Упор достигается на 230 px/с при 60 fps. |
| 2026-09-19 | **Клик-жест добавлен, но не туда, где ожидался.** Лок обязан запрашиваться из жеста, а на title/docked/chart клик принадлежит экрану — значит захват разрешён только при `mousedown` в режиме полёта, слушателем на фазе перехвата на `window`. На титульном экране клик не перехватывается: там указатель остаётся у игрока, иначе первый же клик по «Press M to launch» пропадал бы. |
| 2026-09-19 | **Три блокера ревью закрыты, плюс п. 4, 8, 9, 10, 11, 12 и 13.** Итог: **678 юнитов** (было 650), **81 e2e** (было 63), сборка 721 kB / 196 kB gzip. |
| 2026-09-19 | **Блокер 1: подбор груза был мёртв, канистры текли.** Обработчик убийства звал `dropCargo` и **выбрасывал возврат**, а проверка столкновений ищет канистры в `traffic.ships` — куда они никогда не попадали. Итог двойной: механика «сбей и подбери» не срабатывала ни разу, и каждая канистра висела в сцене навсегда (ни `prune`, ни смена системы её не видели). Заведён `traffic.addWreckage`: канистры идут в тот же список, поэтому их покрывают одним кодом и столкновения, и луч, и `prune`, и `dispose`. Отдельно найден **NaN, о котором предупреждало ревью, и он оказался реальным**: `stepTraffic` не ветвится по типу, `steerToward` считает `s.turnRate * dt`, у канистры `turnRate` нет — `undefined * dt` = NaN, `slerp` пишет NaN в кватернион, и позиция уезжает в `NaN,NaN,NaN` за два секунды. Тест это ловит: на коде без гварда падает. `isTraffic` не даёт обломкам съедать лимит трафика — иначе подобравший пару канистр командир молча лишал систему кораблей. |
| 2026-09-19 | **Блокер 3: семантически битый сейв роняет загрузку.** `loadGame` проверял только парсинг и сид, дальше `deserialize` — безоглядный `Object.assign`. Строка вместо денег роняла экран станции, несуществующая система — вход в систему; и то и другое повторялось **на каждой загрузке**, то есть выбраться из игры было нельзя. Добавлена whitelist-валидация (`PLAYER.validateSave`): диапазоны и конечность чисел, известные товары, предметы и фракции, индексы систем. **Решение — отклонять, а не чинить:** «починенный» сейв означал бы выдуманную историю, а свежий старт на той же галактике — нормальное состояние. Известные наборы передаются аргументом, потому что `player.js` импортирует только `reputation.js` и тянуть в него таблицы товаров было бы первым циклом в проекте. |
| 2026-09-19 | **Ревью предупреждало о NaN — предупреждение подтвердилось на живом коде.** Отдельно от валидации: строка `cash` в сейве делает `g.player.cash` не-числом, и это ровно то, что рвало экран станции. Проверено на стенде: без валидатора все пять битых сейвов загружаются, `stringCash:false` и `badSystem:false` в отчёте; с валидатором — отказ и играбельное состояние. |
| 2026-09-19 | **п. 4: модель дня сведена к одному определению.** День стоит **прыжок** и **стыковка** — ровно два события, и оба зовут `decayDay` (`completeJump`, `dock`). Утверждение «день = стыковка, прыжок бесплатен» было ложным: `enterSystem` строит сцену и ставит корабль на причал, но контрактный стол и сохранение запускает только `dock()`. Код был прав всё время — правились описания. Заодно исправлен тест `a contract never takes longer to fly than its own deadline`: он допускал `hops === days`, а такой рейс приходит на последнем дне и **не имеет дня на стыковку**. Форма `hops + 1` — та же, что уже в правиле курьера (`ceil(hops * daysPerHop) + 1`). Замер: на 400 сидах и ~40 000 предложений нарушений нет ни в мягкой, ни в строгой форме, поэтому ужесточение — страховка от регрессии, а не вскрытый дефект, и так и записано. |
| 2026-09-19 | **п. 8: кулдаун бил по штатной «стыковке — отстыковке».** Стыковка звала `releaseMouse` и взводила те же 1.4 с, что и Escape, — игрок dock→undock через секунду получал мёртвую мышь и подсказку «кликните», то есть **тот же симптом, что и баг, ради которого кулдаун вводили**, но без Escape в сюжете. Заведён тихий выход: стыковка, смерть и карта не взводят задержку, Escape взводит по-прежнему. Ловушка порядка: `exitPointerLock` вызывает `pointerlockchange` **асинхронно**, поэтому сброс флага по таймеру гонялся с событием и проигрывал — флаг теперь снимает сам обработчик. |
| 2026-09-19 | **п. 13: e2e-проверка мыши теперь может упасть.** Она утверждала «у канваса есть `requestPointerLock`» и «обёртку не позвали не на том элементе» — **оба истинны при нуле запросов**, то есть регрессия «игра вообще перестала просить» прошла бы. Теперь счётчик утверждается. Причина нуля оказалась двойной и целиком в проверке, не в игре: (а) игра к этому моменту **уже** держала лок, и `requestMouse` правильно отказывал — проверка теперь сначала отдаёт указатель через новый хуз `releasePointer` и пережидает кулдаун; (б) `g.step` идёт мимо кадровых часов, на которых живёт кулдаун, и они гонялись за одно нажатие — жест теперь идёт через **живой цикл кадров**, как у игрока. Доказано падением: с внесённым «никогда не спрашивать» обе проверки падают с `asked=0`. |
| 2026-09-19 | **п. 15: спасательная капсула была мёртвым контентом.** `dropCapsule` звалась только из теста, ветка `kind === 'capsule'` в `scoop` не могла выполниться. Подключена к тому же событию, что и обломки: убитый торговец или патруль оставляет капсулу. Враждебные не катапультируются — награда за спасение пирата это бой. |
| 2026-09-19 | **п. 9 и 10: смена системы бросала ракеты и цель.** `session.missiles.length = 0` обнулял массив, оставляя геометрию и материал в сцене навсегда, — при том что входящие ракеты строкой ниже чистятся с освобождением. Заведён `dropMissile` (та же тройка «снять, освободить, вынуть»), и `clearMissiles` собран на нём; на него же переведены оба места в `stepMissiles`, где тройка была скопирована. Отдельно: `session.target` переживал прыжок, потому что сбрасывался только по `dead`, а смена системы уничтожает **всю сцену** — цель не мертва, её просто больше нет. HUD рисовал рамку по выброшенному мешу, а ракета в полёте велась за призраком. Обе правки проверены падением: на откаченном коде e2e показывает `targetAfter=object` и `orphansInScene=1`. |
| 2026-09-19 | **п. 11: идентификатор контракта не знал товара.** Форма `тип:откуда:куда:срок:тоннаж` не называла, **что** везут, поэтому два разных груза на одном маршруте с тем же тоннажем и сроком склеивались в один id — и защита от двойного взятия (`accept` отказывает по совпадению id) отвергала вторую по-настоящему другую работу. Товар добавлен в id; у курьера и баунти его нет, и пустой слот оставляет их идентификаторы прежними. Формирование id вынесено в `offerId`, чтобы у идентичности было одно определение, а не строка внутри `makeOffer`. |
| 2026-09-19 | **п. 12: ставка при полном трюме врала.** `(entry && entry.short) \|\| contract.tons` — при полном трюме `short === 0`, ноль ложен, и `\|\|` уходил в запасную ветку, оценивая риск по **всему** тоннажу, хотя докупать нечего. Форма «или» здесь ровно противоположна смыслу: запасное значение нужно только при **отсутствии** записи. Заменено на тернарник. Прежний тест этот край **закреплял как норму** (утверждал полный тоннаж и после загрузки трюма), поэтому он переписан: теперь проверяются полный трюм, частичный и отсутствие товара. |
| 2026-09-19 | **Модель дня во второй раз подтверждена кодом.** Ревью (п. 4) назвало три описания модели; после правки осталось одно, и оно совпало с тем, что уже делал код. Здесь же снят вопрос по п. 7 («подсказка противоречит комментарию»): он был написан против `f389c1a`, а закрыт ещё `f152e95` — `mouseUI.released` держит подсказку **до следующего захвата**, независимо от кулдауна. То же и с блокером 2: кулдаун на любой потере и сброс `lockFailures` при удачном захвате уже стояли в `f152e95`. Ревью читало коммит до правки. |
| 2026-09-19 | **Не тронуто из ревью:** п. 5, 6, 14 и четыре «Совета». Известный мёртвый код рядом с исправленным: `session.scene.rocks` возвращается из `buildSystemScene`, но **не читается нигде** — значит ветка `s.kind === 'asteroid'` в проверке столкновений недостижима так же, как была недостижима канистра. Это не входило в блокер 1 и записано, а не починено молча. |
| 2026-09-19 | **Ветка `main` была рассинхронизирована с удалённой при чистом дереве, и виноват не git, а диск `D:`.** `git status` показывал `[ahead 7]` — семь закоммиченных и **запушенных** правок считались незалитыми, хотя `git ls-remote origin refs/heads/main` возвращал тот же `04ed2aa`, что и локальный `HEAD`. Причина в `RemoveDirectoryW`: **на `D:` вызов `rmdir` по непустой директории возвращает успех и удаляет её вместе с содержимым**, тогда как на `C:` тот же вызов честно отказывает с `ERROR_DIR_NOT_EMPTY` (145). Проверено прямым вызовом Win32: `C:\Temp` → `ok=0 err=145`, `D:\GitHub`, `D:\tmp`, корень `D:` → `ok=1 err=0`, файлы внутри исчезли. Это не NTFS как таковой и не права процесса — оба тома NTFS с одинаковыми флагами. Последствие для git: ссылку он фиксирует, переименовывая `<ref>.lock` поверх `<ref>` и подчищая опустевшие каталоги вверх по пути; на `D:` эта подчистка сносит и **непустой** каталог — `refs/remotes/origin/` исчезала вместе с лежащими в ней ссылками, а сама запись терялась молча, с кодом возврата 0. Отдельным слоем поверх этого: `.git/packed-refs` заканчивался строками с **CRLF**, и git читал последнюю запись как имя `refs/remotes/origin/main\r`, то есть настоящую ссылку не находил. Лечение: файл ссылок перезаписан в LF, обе записи снова на `04ed2aa`. **Урок не про git, а про приём:** `git update-ref` и `git fetch` возвращали 0 всё это время, и «успех» означал лишь «ошибки не было», а не «запись состоялась»; результат надо читать — `git show-ref` и `git ls-remote` — а не полагаться на код возврата. Про репозиторий, живущий на `D:`, стоит помнить: любая операция, которая «подчищает пустой каталог», может унести непустой. Эта запись была переписана после проверки: в первой версии виновником назван перехватчик защитного удаления в инструментарии, и это оказалось неверно — виноват том. Проверялся перехватчик, а не диск, потому что вопрос «а другие каталоги тоже?» был задан последним. **Позднейшая проверка (см. запись «Сага про рассинхронизацию: git здоров, история мутная») этот вывод не подтверждает: замера нет ни скриптом, ни логом, диск `D:` держит 91 репозиторий с рабочими ссылками, и все измерения делались изнутри процесса с перехватчиком. Держать причину как непроверенное наблюдение окружения, а не как знание проекта; симптом и лечение (LF в `packed-refs`, чтение результата вместо кода возврата) от этого не меняются.** |
| 2026-09-19 | **Слияние переводов строк: 2980 строк в диффе оказались ложью прибора.** `src/sim/world.js` показывал 1519 вставок и 1461 удаление при правке в 60 строк — файл был целиком переписан из CRLF в LF более ранним скриптом. Лечится не правкой кода, а возвратом переводов строк: после выравнивания `git diff` даёт ровно 60/2 и совпадает со `git diff -w`. Урок на будущее: цифра в `--stat`, не объяснимая размером правки, — это сигнал смотреть байты, а не верить статистике. |
| 2026-09-19 | **Найдено при отладке: локали захвата — HUD-канвас, у которого `pointer-events: none`.** Лок при этом берётся (браузер не проверяет, получит ли элемент клики), но следующий клик уходит игровому канвасу, и мышь отваливается. Целью стал `#elite-world`. Юнит-тесты этот дефект поймать не могли: они подают фейковый `document`, а `pointer-events` — свойство вёрстки, а не модуля. |
| 2026-09-19 | **Escape перестал отскакивать.** Освобождение ставит паузу `relockDelay` 1.4 с, в течение которой лок не запрашивается: Chrome и сам отказывает в повторном захвате почти сразу после выхода по Escape, так что попытка в следующем кадре — это `pointerlockerror` и мышь, которая «не отпускается». Отказ браузера запоминается в `lockFailed`, и игра перестаёт просить вовсе, а в HUD печатает честное «MOUSE CAPTURE REFUSED BY THE BROWSER». |
| 2026-09-19 | **Подсказка не обещает того, чего нет.** Четыре строки в `HUD.MOUSE_HINT`, и они намеренно **не** проходят через проверку `TITLE_CONTROLS`: та сканирует каждую заглавную букву как обещание клавиши, а «CLICK» и «ESC» — не клавиши игры. Вместо этого отдельный тест сверяет разрешённый словарь строк и связывает `ESC` с `BINDINGS.escapeCapsule`… а точнее — с `leave`. |
| 2026-09-19 | Итог: **650 юнитов** (было 641), **63 e2e** (было 60), сборка 715 kB / 194 kB gzip. Добавленные e2e-проверки считают не захват, а его адресата — headless Chrome не даёт жеста синтетическому нажатию, поэтому проверяется, что запрос уходит элементу, способному держать лок, а не `window`. Сам захват в headless не проверяется, и это записано, а не замолчано. |
| 2026-09-18 | **#9 закрыт по-настоящему: причина оказалась в приборе, и дефектов было четыре.** Прошлая сессия записала гипотезу про асимметрию расходов; потоковый разбор её опроверг (расходы симметричны), но и первую формулировку («виноват срок контракта») пришлось уточнить: срок в игре работает верно. Настоящие причины — четыре дефекта `economy-sim.mjs`. |
| 2026-09-18 | **(1) Часы.** `runCareer` тратил день на каждый прыжок. Симулятор был **прав**, а не ошибался: прыжок в игре действительно стоит дня (`decayDay` зовётся и из `completeJump`, и из `dock`). Прежняя запись утверждала обратное — что прыжок бесплатен, — и на этом ложном основании симулятор «чинили». Причина расхождения по длинным плечам не в часах, а в том, что `MISSION.days` считает рейс как «стыковка на дальнем конце» и не закладывает дни в пути. Заведён `dockAndAdvance()`: день тикает и на прыжке, и на стыковке, контракты проверяются на новом дне, порядок как в `dock()` — сдача до продажи груза, дедлайн включительный. |
| 2026-09-18 | **(2) Зачистки не начинались.** Заказ на зачистку указывает собственную систему, прыжка не требует. Симулятор ставил цель и уходил в поиск торгового рейса, где искал соседа, сокращающего расстояние до цели в 0 ly, не находил и улетал — заказ горел в порту, где был взят. Трассировка показала это дословно: «accepted day 1, deadline 9, still in the same system on day 10». После правки зачисток стало 16 вместо 7, провалов 7 → 2. |
| 2026-09-18 | **(3) Груз покупался один раз.** Ветка покупки стояла под `!player.cargo[carry.id]`, поэтому отказывалась добирать недостающее, если хоть что-то уже лежало. Это и был настоящий корень «невыполнимых доставок»: корабль катался Durice→Zaon→Durice, покупая 8 из 13 т, потом 5 из 13, и никогда не набирал нужное. Теперь добирает до тоннажа контракта, как `MISSIONS.shoppingList`. |
| 2026-09-18 | **(4) Заказ, выполненный на месте, не сдавался.** `resolveArrival` вызывался в конце прохода, по следующей системе, поэтому доставка в текущую систему не имела ни прыжка, ни стыковки, где её оплатят. Карьера стояла на выполненном заказе 102 дня (Duanor: 7 т food в трюме, 26 т на местном рынке, ноль соседей в радиусе бака). Добавлена ветка сдачи на месте; счётчик `delivered in place` в отчёте показывает её вклад — 135 стыковок за 800 прыжков. |
| 2026-09-18 | **Проверено, что игра была права.** Утверждение «заказ просрочивается, пока корабль в чужом порту» оказалось ложным: `resolveArrival` проверяет `day > deadlineDay` **выше** гварда `targetIndex`, то есть срок истекает при любой стыковке, где бы ни был командир. Четыре написанных под это теста (истечение в чужом порту, идемпотентность, последний законный день, abandon) прошли на текущем коде — дефекта нет, тесты откачены, чтобы не тащить покрытие несуществующей проблемы. Дефект был в симуляторе, а не в `missions.js`. |
| 2026-09-18 | **Найдены два настоящих дефекта доски — четвёртый и пятый барьер.** (а) **Заказ больше склада:** 1387 из 3080 грузовых предложений (45 %) требовали больше товара, чем есть в системе-отправителе, 120 — товара, которого там нет вовсе; худший разрыв 18 т. Доставка покупается на месте, поэтому такой заказ невыполним в принципе. Добавлен `suppliedStock(system, comId, day, player)` — читает настоящий рынок, а не приближение, чтобы доска и экран рынка не расходились. Полоса тоннажа сдвигается вниз при нехватке склада, а не поднимается: бедный мир должен выдавать мелкий заказ, а не невозможный. (б) **Курьер вне своего срока:** Rari → Ores, 6 прыжков при 5 днях. Курьер — единственный заказ, который выбирает цель по *максимуму* расстояния, а срок рисовался независимо от него. Теперь срок поднимается под маршрут (`daysPerHop: 1` плюс стыковка на дальнем конце), а не опускается: опустить значило бы выдавить длинные рейсы с доски. |
| 2026-09-18 | Почему (б) не был виден раньше: он вылез только после правки (а), потому что кламп изменил поток `rand()` и сдвинул всю доску. Это ровно тот случай, когда правка генератора вскрывает соседний дефект — и почему тест на «срок не короче маршрута» стоит держать общим для всех типов, а не только для доставки. |
| 2026-09-18 | **#13 переписан заново: прежний замер был дефектным, и он переворачивал вывод.** Старая цифра «relief 129 > courier 113 > bounty 87 > delivery 44 CR/день» делила награду на **полосу `days`** — то есть на *срок*, а не на стоимость поездки. Для зачистки это грубейшая ошибка: её цель в собственной системе, поездка занимает одну стыковку, а срок дан 9 дней — значит делили на число, которое к зачистке отношения не имеет. |
| 2026-09-18 | Честный замер (**2560 досок, 5240 предложений**): дни поездки считаются как «прыжок стоит дня плюс стыковка на дальнем конце» (в игре день тикает на стыковке — ровно то, что чинили в симуляторе), и вычитается себестоимость груза, потому что грузовой контракт надо купить. Чистыми за день: **bounty 740** (100 % маржи), **relief 175** (89 %), **courier 95** (100 %), **delivery 53** (72 %). |
| 2026-09-18 | **Вывод переворачивается.** Прошлая сессия записала: «зачистка выигрывает не наградой, а доступностью». Честный замер говорит обратное — она выигрывает **и наградой, и доступностью**: в **14 раз больше доставки за день** и при этом берётся на **66 % досок против 22 % у relief и 23 % у курьеров**. Формулировка «не наградой» была артефактом деления на срок. Приоритет поднят **P3 → P1**: пока это не решено, у доски есть один правильный ответ и он всегда один и тот же. |
| 2026-09-18 | Отдельно измерена **частота по типам** (сколько из 64 досок содержат тип): delivery 94 %, bounty 66 %, courier 23 %, relief 22 %. Это уточняет #11: «курьеры редки» верно по абсолютной частоте, но relief — редчайший тип, а не курьер. |
| 2026-09-18 | Тесты: `missions.test.js` (+1) — «грузовой заказ просит не больше, чем может дать система». На прежнем поведении падал со **147 невыполнимыми** на срезе четырёх дней. Итого **641 юнит, 60 e2e**, сборка 712 kB. |
| 2026-09-17 | **Таблица Open-проблем разобрана: закрытые строки уехали в архив ниже.** Таблица из двадцати строк, где семнадцать закрыты, — это не список задач, а свалка: закрытая строка рядом с открытой выглядит как незакрытая. Осталось четыре открытых (#8, #9, #11, #13) по номерам. Закрытые #1–#7, #10, #12, #14–#16 перенесены сюда без потери содержания. |
| 2026-09-17 | **#9 переписан: «торговля вдвое сильнее» — артефакт прибора.** Потоковый разбор `economy-sim.mjs` (отдельный пробник, считающий каждое движение денег) показал, что расходы симметричны: топливо, штрафы и оборудование вычитаются из обеих карьер, а торговля не получает груз бесплатно. Настоящая причина — **модель дня**. Симулятор тратит день на каждый прыжок, тогда как `MISSION.days` рассчитаны на игровую модель «день = стыковка» (`decayDay` вызывается в `dock`; прыжок по времени бесплатен). Поэтому на длинных плечах заказы горят: **35 провалов из 58** принятых в колонке «сначала грузовые», доход по контрактам **−1 727 CR** за 120 прыжков и **−18 975 CR** за 800. |
| 2026-09-17 | **Проверка замером, а не рассуждением:** на 800 прыжках колонка «по максимальной награде» **обгоняет** торговлю — 72 280 против 69 516 CR. То есть заголовок скрипта («на 200 прыжках колонки сходятся — 14 201 против 17 482») прав по выводу и неверен по числам: сходимость наступает позже и по другой причине. Формулировка «контракты дают 66 % дохода» описывает **долю в выручке**, а не итоговый капитал, и читается как «контракты выгодны», хотя в тех же прогонах итог был ниже. Обе фразы в заголовке переписаны. |
| 2026-09-17 | **Отдельный дефект, найденный тем же разбором: срок контракта не проверяется, пока командир не вернётся в целевую систему.** Зачистка занимает систему на N дней (`clearing.daysLeft = tons + 1`), но `resolveArrival` откладывает проверку `day > c.deadlineDay` до совпадения `c.targetIndex === systemIndex`. Заказ на 6 тонн с дедлайном 6 дней успевает просрочиться, пока корабль стоит в чужом порту. Это дефект, а не баланс: просроченный заказ должен истекать в день дедлайна, где бы ни был командир. |
| 2026-09-17 | Запас срока на живых досках измерен и **щедрый**: 55 заказов из 3 680 не оставляют времени на круговой рейс. То есть в игре окно не жмёт — жмёт только модель дня в симуляторе. Это подтверждает, что чинить надо прибор и проверку срока, а не трогать `MISSION.days`. |
| 2026-09-17 | **Закрыты все три барьера доски (#14, #15, #16) — без смены дизайна, с расширением видимости.** Доска выдавала работу, до которой нельзя долететь, больше тонн, чем влезает в трюм, и молчала о том, что груз надо купить самому. Механика во всех трёх случаях осталась прежней: изменён **отбор** того, что предлагается (граф маршрутов, трюм) и **что об этом сказано** игроку. |
| 2026-09-17 | **#14. Достижимость переехала из симулятора в логику.** `hopsBetween(galaxy, from, to, fuel)` теперь живёт в `src/logic/galaxy.js` — обход в ширину **по графу маршрутов**, ребро проходимо, только если его длина в световых годах укладывается в полный бак (ровно то, что проверяет `canJump`). Раньше та же функция была только в `scripts/economy-sim.mjs`, поэтому генератор доски про достижимость «не знал», а симулятор знал — и оба расходились. |
| 2026-09-17 | Порог `MISSION.reach: 90` (прямая, ×`lyPerUnit`×1.6 = 28 ly при баке 7) заменён на `MISSION.maxHops: 6`. Шесть — потому что день стоит одну стыковку, а рейс туда-обратно тратит два прыжка бюджета. Замер после: из **1088 предложений — 0 недостижимых**, и ни одно не требует больше прыжков, чем у него дней. Дальняя работа не исчезла — она просто стала летабельной. |
| 2026-09-17 | **Тест, который действительно падал.** «Каждая цель достижима при полном баке» на прежнем поведении упал с **541 недостижимой из 768** — то есть тест проверяет поведение, а не пересказывает код. Второй тест («контракт не длиннее своего срока») ловит перебор прыжков над днями. |
| 2026-09-17 | **#15. Потолок тоннажа клампится трюмом:** `Math.max(minTons, Math.min(maxTons, holdMaxOf(player)))`. Проверено откатом — на прежнем поведении тест падает с **54 из 706** грузовых предложений, не влезающих в трюм. Расширенный трюм сохранил смысл: он поднимает потолок *самой доски*, а не остаётся единственным способом взять уже выданное. |
| 2026-09-17 | **#16. Механика оставлена, барьер раскрыт.** Покупка груза на рынке — задуманный путь, а не недоделка: товар всегда берётся из излишков системы (145/145), а не выдаётся контрактом. Недостающим было **сообщение**, и теперь его три: `MISSIONS.shoppingList(player)` — что докупить (ключом по товару, чтобы два контракта на еду не удваивали список); пометка `NEEDED: N t` на строке рынка (жёлтая, если ещё не хватает, зелёная, если набрано); панель «NEEDED FOR CONTRACTS» под сводкой; и строка на доске: «Cargo contracts do not load your hold — they check it on arrival». |
| 2026-09-17 | **Добавлено на доску то, что отвечает на настоящий вопрос игрока: число прыжков.** Дистанция в световых годах его не решает — корабль прыгает по рёбрам, поэтому цель в 4 ly может быть в трёх прыжках, а в 9 ly — в одном. `hops` теперь несётся самим предложением (генератор уже считает его при отборе), колонка **Jumps** стоит рядом с **Days**, а у живого контракта под строкой печатается `at risk if overdue: N CR` — штраф **плюс** груз, потому что штраф в 35 % награды (потолок 600) систематически занижает цену провала. |
| 2026-09-17 | Проверено в собранной игре через `window.__ELITE_GAME__`: взял грузовой контракт — на рынке появилось `NEEDED: 16 t`, в панели `Food 16 t of 16 t`, предложения несут `hops` 1, живой контракт — `stake 72`. Снимки вкладок Contracts и Market подтвердили вёрстку. Итого **640 юнитов + 60 e2e**, сборка 712 kB. |
| 2026-09-17 | **Найдено при разборе доски контрактов: 68 % целей недостижимы.** Отбор цели в `generateBoard` — `c.dist <= MISSION.reach * lyPerUnit(galaxy) * 1.6`, то есть **28 световых лет** при баке **7**. Замер на живом графе маршрутов (192 предложения, все 64 доски): напрямую недостижимы **все 192**, а при полном баке цель недостижима у **131 (68 %)** — delivery 115/133, relief 11/12, courier 5/5. Из Lave достигается 7 систем из 64; прямые соседи 0.99…9.1 ly, седьмой уже выше бака. Диаметр галактики 98 ly при самом длинном маршруте 20.5 ly. |
| 2026-09-17 | Комментарий над строкой утверждает обратное: «Neighbours within reach, richest first, so the board prefers a target the commander can actually get to on one tank». Множитель `* 1.6` при `reach = 90` даёт 28 ly, а не 7 — комментарий описывает намерение, которого код не выполняет. Тот же класс, что уже случался в `rng.js` и в подсказках станции. |
| 2026-09-17 | **Почему этого не видит ни один юнит-тест.** Доска собирается из фикстур галактики, а проверка достижимости требует настоящего графа прыжков и бака — то есть прогона со `canJump`. Пробник, который это нашёл, строит граф из `galaxy.routes`, взвешивает рёбра реальной дистанцией в световых годах и обходит его как достижимость по топливу. |
| 2026-09-17 | **Второй барьер, найденный тем же замером: тоннаж против трюма.** 11 из 145 грузовых предложений требуют больше 20 тонн (`tons.delivery = [6, 22]`, `tons.relief = [10, 30]`), при базовом трюме 20 и расширенном 35. Границы не сверяются с `holdMaxOf`: расширенный трюм покупается ровно ради этих 11 предложений, и доска об этом не сообщает. |
| 2026-09-17 | **Третий барьер: сдача доставки требует груза, которого выдавший контракт не даёт.** `resolveArrival` сдаёт работу только при `held >= c.tons`, но груз не кладёт ни один путь взятия контракта — `addCargo` вызывается лишь из покупки на рынке и подбора обломков. Товар при этом **всегда** есть в списке рынка и **всегда** из излишков системы-отправителя (145/145), так что покупка — задуманный путь; но сток покрывает тоннаж только в **62 из 145 (43 %)**. Доска не говорит ни о необходимости покупки, ни о том, что стока может не хватить. |
| 2026-09-17 | Тест «cannot complete without the goods» **корректен, но держит фикстуру, в которой груз уже лежит в трюме**, — поэтому ни один юнит-тест не видит этот класс. Ровно то, о чём предупреждает AGENTS.md: «не пиши фикстуру, которой не бывает в реальности». |
| 2026-09-19 | **Модель дня сведена к одному определению.** День стоит **прыжок** и **стыковка** — ровно два события, и оба зовут `decayDay` (`completeJump`, `dock`). Утверждение «день = стыковка, прыжок бесплатен» было ложным: `enterSystem` строит сцену и ставит корабль на причал, но контрактный стол и сохранение запускает только `dock()`, поэтому прилёт в систему — это прыжок, а не стыковка. Три места описывали модель по-разному; канон теперь в докстринге `decayDay`, а неверные ссылки исправлены. Прошлые замеры доски делили награду на «стыковки» и потому переоценивали дальние плечи: рейс на три прыжка стоит не один день, а три. |
| 2026-09-17 | **Награда за день пересчитана на живых досках** (было только в симуляторе): relief 129 > courier 113 > bounty 87 > delivery 44 CR/день. Прежняя версия этой строки утверждала «один день стоит одну стыковку, прыжок бесплатен» — **это было неверно** и занижало стоимость дальних рейсов; см. запись за 2026-09-19. |
| 2026-09-17 | **Открытая проблема #13 переформулирована замером.** «Зачистки доминируют по награде» механизм не подтверждает: bounty платит 87 CR/день против 129 у relief и 113 у courier. Зачистка выигрывает **доступностью** (42 доски из 64, единственная без цели вне системы) и отсутствием барьеров — нет покупки груза, нет прыжка, нет дефицита товара. Симулятор `economy-sim.mjs` этой асимметрии не видит, потому что не знает ни про достижимость, ни про тоннаж. |
| 2026-09-17 | Ничего не правилось: три барьера измерены и записаны открытыми проблемами #14 (P1), #15 (P2), #16 (P2). Правка требует решения владельца о том, чем считать «достижимость» (бак, апгрейд топлива, число прыжков с дозаправками) и что делать с недостижимыми целями — не выдавать вовсе или выдавать с явным числом прыжков и топлива. |
| 2026-09-17 | Проверка состояния на входе: **628/628 юнитов, 60/60 e2e, сборка 709 904 Б**, ноль ошибок в консоли, коммит `560a766`, дерево чистое. |
| 2026-09-17 | **Аудит сохранения: найдено, что последняя станция не сохранялась.** `serialize` писал 24 поля из 29. Проверка кругом «сохранить → загрузить» по всем полям сразу: `dockedAt` возвращался `null` вместо записанного значения. |
| 2026-09-17 | **Следствие серьёзное и видимое игроку.** `enterSystem` при гибели откатывает на `dockedAt === null ? 0 : dockedAt`, а экран смерти обещает «rescued at your last station». То есть командир, прошедший полгалактики, сохранившийся и перезагрузившийся, после гибели попадал **на Lave**. Плюс загрузка открывалась на заголовке вместо экрана станции: `setMode` выбирает режим именно по этому полю. |
| 2026-09-17 | Исправлено: `dockedAt` пишется и читается. Для сохранений, записанных до этой правки, `undefined` трактуется как `null` — «ещё не стыковался». |
| 2026-09-17 | **Остальные «потери» оказались безвредными, и это проверено, а не предположено.** `energy` и `heat` восстанавливаются сами, `energyMax` и `missileMax` — константы. `equip` и `standing` пробник пометил ложно: `deserialize` намеренно **сливает** их с умолчаниями, поэтому замена объекта целиком выглядела как потеря, хотя поле в порядке. |
| 2026-09-17 | Тесты: `player.test.js` (+1) — **аудит кругом по всем полям записи**. Четыре транзитных поля названы **списком**, а не отфильтрованы шаблоном: чтобы пятое стало чьим-то решением, а не молчаливым пропуском. Отдельная проверка требует, чтобы каждое имя из списка всё ещё существовало в записи — иначе переименование поля оставит исключение, которое ничего не покрывает. e2e (+1): сохранение помнит последнюю станцию. Итого 625 юнитов, 60 e2e. |
| 2026-09-17 | **Найден флейк — и вместе с ним нарушение правила проекта.** Прогон полного набора упал на тесте «пламя реагирует на вираж», который до этого проходил. Пять изолированных прогонов `world.test.js` — зелёные, то есть тест **недетерминирован**, а не сломан. |
| 2026-09-17 | Причина: `patrolPoint` выбирает точку обхода через **`Math.random()`**, и это ровно то, что запрещает правило в шапке `rng.js` — «никогда не используй `Math.random()` для того, что игрок может наблюдать дважды». Патрулирующий корабль берёт случайную точку, когда истекает таймер блуждания; тест пламени измеряет качание носа, поэтому результат зависел от того, куда его занесло. |
| 2026-09-17 | Всего в симуляции было **четыре** места с `Math.random()`: точка обхода, перезарядка ракеты, перезарядка пушки и разброс выстрела. Заменены на собственный seeded-поток трафика (`traffic.rand`), тот же, которым уже пользуется спавн. Остальные `Math.random` в проекте — тряска камеры и рециркуляция пыли — косметика, их не трогал. |
| 2026-09-17 | **Доказано, а не проверено глазами.** 600 кадров симуляции при `Math.random`, заменённом на **бросающий исключение**: ноль вызовов. И два одинаковых прогона дают побитово одинаковый результат (`cruise 0.19381959851282096`, `banking 0.47803568233112387`, позиция совпадает до последнего знака). |
| 2026-09-17 | Тесты: `world.test.js` (+2) — два одинаковых прогона одного трафика дают одинаковый результат; симуляция проходит 120 кадров с бросающимся `Math.random`. Второй тест ставит заглушку **после** создания сцены: three.js зовёт `Math.random` в `generateUUID` для каждого `Object3D`, и глобальная заглушка упала бы внутри three, а не внутри ИИ. |
| 2026-09-17 | **Почему это важно за пределами тестов:** невоспроизводимый ИИ означает, что баг в поведении корабля нельзя повторить, а значит нельзя и починить. Детерминизм — это не про тесты, а про возможность отладки. |
| 2026-09-17 | **Прослежен первый запуск — и найдено, что вводные сообщения сгорают, пока игрок читает заголовок.** Замер: сразу после загрузки в журнале 3 записи («Arrived: Lave», приветствие фракции, слух), **через 10 секунд на заголовке — 0**, а после нажатия M игрок оказывается в космосе с одной строкой «Undocked from Lave Station». То есть игра говорит что-то при входе и стирает это до того, как игрок вошёл. |
| 2026-09-17 | Причина в комментарии, который объясняет замысел: «Age messages regardless of mode so they do not pop when a menu closes». Замысел верен для **меню** — если сообщения старели бы только в полёте, стыковка и отстыковка заставляли бы строку перескочить весь свой срок за один кадр. Но **заголовок — это не пауза в игре, а время до неё**: командир читает там управление столько, сколько хочет. |
| 2026-09-17 | Правка: сообщения не стареют в режиме `TITLE`, во всех остальных — как раньше. Замер после: через 10 секунд на заголовке **3 записи на месте**, после запуска **4** («Arrived: Lave», приветствие, слух, «Undocked from Lave Station»). |
| 2026-09-17 | Попутно проверено и **признано безвредным**: `player.dockedAt` остаётся `null` после запуска со станции Lave. При гибели игра откатывает на `dockedAt === null ? 0 : dockedAt`, а система 0 — это и есть Lave, откуда игрок стартовал. Ложная тревога, закрыта чтением кода. |
| 2026-09-17 | e2e (+2): сообщение переживает 15 секунд на заголовке **и** нормально истекает после 6.5 секунд полёта — вторая половина нужна, иначе проверка прошла бы на игре, где сообщения не стареют вообще. Итого 59 e2e-проверок. |
| 2026-09-17 | **Замер производительности: оптимизировать нечего.** Никогда не мерил, сколько процессора стоит кадр, — и это решает, играбельно ли на слабом ноутбуке. Замер в браузере (600 кадров после прогрева, 1440×810): |
| 2026-09-17 | **Полёт, только логика — 0.03 мс/кадр** (потолок 33 000 fps). Логика + рендер — **3.86 мс** (259 fps), причём под **SwiftShader**, программным растеризатором, то есть на настоящей видеокарте быстрее. **Гнездо с семью враждебными — те же 0.03 мс**: ИИ не стоит ничего. |
| 2026-09-17 | То есть логика занимает **0.2 %** бюджета кадра в 16.7 мс. Это отрицательный результат: список «что оптимизировать» пуст, и записан он затем, чтобы следующий читатель не искал проблему, которой нет. |
| 2026-09-17 | В e2e добавлен страж: длинный прогон на 2000 кадров теперь замеряется и падает, если кадр стоит больше 4 мс. Порог намеренно свободный — в сто раз выше измеренного, — чтобы ловить катастрофу (случайный проход O(n²), аллокация объекта сцены в кадре), а не колебания машины. Итого 57 e2e-проверок. |
| 2026-09-17 | **Найдено на снимке карты: её фон гасил не только мир, но и HUD.** `drawChartOverlay` вызывался **последним**, поэтому его затемнение ложилось поверх колонки статуса, компаса и журнала сообщений. Замер: мир за картой менял **27 % площади данных** (3.7 % заметно) — сквозь граф маршрутов просвечивали рёбра станции, самые яркие линии в кадре. |
| 2026-09-17 | Первая попытка была неверной: поднял непрозрачность 0.86 → 0.96, и это убрало просвечивание (27 % → 6 %), **но сделало колонку статуса почти невидимой** — потому что фон гасил её и раньше, просто слабее. Оба симптома имели одну причину: фон закрывал не тот слой. |
| 2026-09-17 | **Правильная правка — развести слои, а не крутить число.** `drawHud` теперь считает `chartUp` один раз и: не рисует приборы полёта (прицел, сканер, рамку цели, компас, дуги урона) — на карте они бессмысленны; рисует карту **до** колонки статуса и журнала; оставляет статус, опознание, журнал и предупреждения поверх карты. |
| 2026-09-17 | Легенда карты перенесена из левого нижнего угла в центр низа: журнал сообщений рисуется **после** карты и перекрыл бы её. Сканер, занимавший низ по центру, на карте теперь не рисуется — место свободно. |
| 2026-09-17 | Тесты: `hud.test.js` (+2) — карта рисует меньше, чем кабина; колонка статуса на карте на месте; фон карты рисуется **до** статуса, а не поверх (проверяется индексом вызова `fillRect(0,0,w,h)` против индекса первой подписи `SHLD`); легенда центрирована и не сталкивается с журналом. Итого 622 юнита. |
| 2026-09-16 | **Закрыта открытая проблема #12: взятый контракт больше не показывается дважды.** Было: задание оставалось на доске тусклой строкой «accepted» и тут же повторялось живой строкой «in progress / abandon». Теперь взятые предложения отфильтрованы — задание видно **один раз**, в разделе своих. Замер в браузере: принял контракт на Durice, на доске 3 строки, Durice упоминается **один** раз. |
| 2026-09-16 | Курсор остался один над одним списком (доступные предложения, затем взятые) — это и было обоснованием исходного решения, и оно сохранено. Изменилось только число строк: одна на задание. Последняя колонка теперь читается единообразно — она говорит, что сделает Enter: «accept» у доступных, «abandon» у взятых. |
| 2026-09-16 | Проверка `offer.taken` убрана из `activateContract` как недостижимая: взятого предложения в списке больше нет. Настоящая защита от двойного взятия — в `MISSIONS.accept`, которая отказывает по id, что бы ни думал экран. |
| 2026-09-16 | Тесты обновлены осознанно, а не подогнаны: «Enter по взятому предложению отказывает» заменён на «взятого предложения в списке нет вовсе»; тесты переноса курсора пересчитаны с четырёх строк на три. **Третий случай за сессию, когда тест утверждал прежнее поведение как требование** — см. ADR. |
| 2026-09-16 | **Третья ложная тревога за раунд.** Строка взятого контракта на снимке выглядела тусклее доступных. Замер `getComputedStyle`: у обеих строк `rgb(159,232,255)` и `opacity 1` — **разницы нет**. Взгляд сравнивал их с подсвеченной выбранной строкой выше и с тусклой подписью «in progress». Проверять надо вычисленный стиль, а не впечатление. |
| 2026-09-16 | **Репозиторий опубликован и CI нашёл в нём дефект, который локально не виден.** `github.com/AlexanderKuzikov/Elite-Deep`, публичный, ветка `main`. Первый прогон CI: **юниты и сборка прошли, e2e упал** — `Cannot find module 'D:/GitHub/node_modules/puppeteer-core'`. |
| 2026-09-16 | **Скрипт e2e зависел от пути на машине автора.** `require('D:/GitHub/node_modules/puppeteer-core')` — абсолютный путь, который работал только потому, что пакет лежал в **родительском** каталоге проекта. У любого, кто клонирует репозиторий, `npm run e2e` падал бы с той же ошибкой. `puppeteer-core` объявлен в `devDependencies`, импорт — обычный bare specifier. **Путь, который разрешается на одном компьютере, — это не зависимость.** |
| 2026-09-16 | Почему это не поймали раньше: локально пакет находился из родительского каталога, поэтому e2e работал. Проверка «локально всё зелёное» не отличает зависимость от совпадения — отличает её только чистый прогон на другой машине. |
| 2026-09-16 | GitHub Pages включён через API (`build_type: workflow`): игра доступна по `https://alexanderkuzikov.github.io/Elite-Deep/` — один самодостаточный HTML, без установки. |
| 2026-09-16 | **Репозиторий подготовлен к публикации.** `git init -b main`, 64 файла, первый коммит. `.gitignore` расширен (мусор редакторов и ОС, `node_modules`/`dist`/`e2e-shots` остаются исключёнными). Добавлены `LICENSE` (MIT + оговорка о товарном знаке), `package.json` с `license`/`engines`/`keywords` и скриптом `npm run sim`. |
| 2026-09-16 | **Найдена и исправлена ошибка, которая сломала бы README на GitHub.** Скриншоты лежали в `e2e-shots/` — папке, которую прогон e2e перезаписывает и которая в `.gitignore`. То есть на GitHub все пять картинок README были бы битыми. Сделан отдельный набор из семи снимков в `docs/screenshots/` (1440×810), который коммитится, а `e2e-shots/` остаётся черновиком. |
| 2026-09-16 | **CI.** `.github/workflows/ci.yml` — два задания: юниты + сборка и e2e в headless Chrome (скриншоты прогона уходят артефактом). `.github/workflows/pages.yml` собирает и публикует игру на GitHub Pages: один самодостаточный HTML открывается по ссылке без установки. Поиск Chrome в `e2e.mjs` научен переменной `CHROME_PATH` и путям Linux/macOS — иначе на раннере он бы не нашёлся. |
| 2026-09-16 | **Найдено на скриншоте для README: планета выглядела как гранёный кристалл.** На её поверхности читалась **решётка value-шума** — шестиугольные ячейки со прямыми краями. Комментарий в `rng.js` утверждал обратное: «визуальной разницы на планете при таком бюджете треугольников нет». Замер и снимок показали, что разница есть, и дело не в бюджете, а в природе шума. |
| 2026-09-16 | `noise3` переписан с **value-шума на градиентный (Перлин)**: значение в узлах решётки ровно ноль, между ними — плавное поле, ячейкам взяться неоткуда. Комментарий исправлен на честный. Тесты переписаны: старый тест «нет мёртвых линий решётки» проверял *свойство value-шума*, и с градиентным шумом падал законно — теперь он утверждает, что поле ноль в узлах и гладко вокруг них. |
| 2026-09-16 | Побочно найдено свойство классического набора из 12 градиентов: в **центре ячейки** поле может быть ровно нулём — замер, 39 из 200 зёрен. Это одна точка на ячейку с ненулевым градиентом через неё, а не плоская область, поэтому невидимо. Записано в комментарии к функции: больший набор градиентов это убрал бы, но не стоит таблицы. Тест «шум различается по зёрнам» переведён с одной точки на пятьдесят — иначе он падал на этом совпадении, а не на ошибке хеша. |
| 2026-09-15 | **Симулятор экономики научился считать контракты и оборудование** — закрыта последняя открытая проблема, которую можно было закрыть без решения владельца. `economy-sim.mjs` теперь прогоняет **три карьеры**: только торговля, торговля + контракты (берёт работу с максимальной наградой) и торговля + контракты (сначала грузовые). Третья колонка существует, чтобы **проверить инструмент**, а не чтобы моделировать игрока: если грузовые работы не выполняются, когда их выбирают первыми, значит сломан путь груза, а не «они невыгодны». |
| 2026-09-15 | **Ответ на вопрос, который инструмент не мог задать: контракты — не гарнир.** У командира, который их берёт, они дают **до 66 % дохода** (120 прыжков: 14 286 CR контрактами против 7 513 торговлей). Но **карьера заканчивает с меньшими деньгами, чем чистая торговля** — 5 492 против 10 627, — и обе причины не в том, что контракты плохо платят: разовая покупка оборудования **11 800 CR** (шесть предметов) и **81 день из 120, проведённый в зачистке**. Стоять на месте не зарабатывает. |
| 2026-09-15 | **И это верно только для первых ста прыжков.** На 200 прыжках разрыв почти закрывается: 14 201 против 17 482, рост 0.56 % против 0.73 % за прыжок — оборудование окупается и продолжает работать. Формулировка «брать работу = беднеть» верна для ранней игры и неверна дальше, а это другое и гораздо более полезное предложение. |
| 2026-09-15 | **Зачистки доминируют по награде.** Жадный по награде командир берёт **16 зачисток и ни одной грузовой работы**: зачистка платит больше одного торгового рейса, а модель считает её как «один день на пирата». За 200 прыжков он проводит **136 дней** в зачистке. Остальные три типа контрактов строго хуже по награде за день. Записано открытой проблемой #13: в реальной игре за это платят риском, но на бумаге выбора нет. |
| 2026-09-15 | **Своя ошибка, найденная прогоном: две версии стратегии подряд мерили стратегию, а не экономику.** Первая брала работу, как только руки свободны, — набрала четыре и обслуживала одну, семь провалила, штрафы съели прибыль. Вторая держала позицию при активной зачистке **до того, как дошла до цели**, поэтому дни не тикали и корабль навсегда вставал не в той системе (финальный капитал — 1 CR). Инструмент врёт так же охотно, как стенд. |
| 2026-09-15 | Побочно: `hopsBetween(from, target, fuel)` — поиск в ширину по графу прыжков. Без него командир берёт работу, до которой не успевает долететь, и платит штраф. Фильтр «успею ли» сделан по **полному баку**, что оптимистично (дозаправка стоит денег и крюка), — отсюда 5–9 провалов в грузовой колонке. Это записано в шапке скрипта, а не спрятано. |
| 2026-09-15 | **Найдено на снимке вкладки Contracts: за экраном станции рисовался полётный HUD.** Экран станции — полноэкранная накладка со своей сводкой, но за ней оставался HUD полёта: прицел висел поверх таблицы рынка, кольцо сканера просвечивало через список цен, слева угадывались полосы щита и топлива. Замер: полётный HUD красил **2.9 % кадра** в доке. |
| 2026-09-15 | **Причина — провал в цепочке условий.** HUD рисовался во **всех** режимах, кроме title, dead и hyperspace, то есть `docked` попадал в `else` молча. Решение вынесено в чистую функцию `hudOverlayFor(mode)`, возвращающую `title` / `death` / `hyperspace` / `flight` / `none`. Список режимов, которые что-то рисуют, честнее провала: провал незаметно включает всё новое. `chart` **не** в списке молчащих: карту рисует сам HUD, этому режиму канвас нужен. |
| 2026-09-15 | Замер после правки — по пикселям самого канваса HUD, а не по скриншоту: **flight 6.64 % (61 214 px), docked 0.00 % (0 px)**, chart 100 %, title 100 %. До правки `docked` шёл по ветке `flight`, то есть красил те же 6.64 %, из которых через полупрозрачную панель просвечивало ~44 %. |
| 2026-09-15 | Попутно подтверждено, что доска зачисток работает: в опасной системе Ceanla доска — `[delivery, delivery, bounty]`, «Clear 6 pirates from Ceanla» за 1057 CR против 206 и 197 у доставок. Спокойные системы зачистку не предлагают. |
| 2026-09-15 | Тесты: `hud.test.js` (+1: полётный HUD не рисуется в доке; остальные режимы сохраняют своё; `chart` намеренно остаётся на канвасе). Итого 620 юнитов. |
| 2026-09-15 | **Найдено на снимке заголовка: «1 systems visited».** Первое предложение, которое читает новый командир, было с неверным числом. Та же ошибка на экране смерти: «1 kills». Добавлен один общий помощник `HUD.countOf(n, noun)` вместо трёх тернарников на местах; проверено, что третье место (сообщение об очистке розыска) уже обрабатывало единственное число само. |
| 2026-09-15 | **Отрицательный результат: тёмная станция на заголовке — не дефект.** На снимке заголовка станция выглядит тёмным проволочным многогранником, и это выглядело тем же классом, что пояс до правки. Замер развеял: со стороны звезды станция даёт **среднюю яркость 78.6 и 84 % пикселей выше 35** (плотное светлое тело), с теневой стороны — 24.4 и 18.9 %. Материал в порядке, дело в ракурсе. А ракурс на заголовке **не фиксирован**: камера медленно облетает станцию по кругу (радиус 3.2 её радиусов, 0.12 рад/с), проходя и светлую, и теневую сторону. Тёмным центр кадра делает **собственный градиент экрана заголовка** (`rgba(2,4,10,0.86)` в центре) — он там ровно для того, чтобы читался текст. Ничего не менял. |
| 2026-09-15 | Это второй раз за сессию, когда замер отменил правку, и оба раза — по одной причине: снимок показывает **результат**, а не **причину**. Тёмные камни были тёмными из-за альбедо; тёмная станция тёмная из-за градиента поверх неё. |
| 2026-09-15 | Тесты: `hud.test.js` (+1: счёт из единицы не во множественном числе). Итого 619 юнитов. |
| 2026-09-15 | **Найдено на снимке карты: легенда обещала клавишу, которой нет.** Внизу карты было написано «**ENTER = select**», а `Enter` не привязан **вообще нигде** — ни в `BINDINGS`, ни отдельным обработчиком. Карта управляется стрелками, `T`/`G` (шаг по системам) и `H`/`J` (прыжок). Тот же класс, что «Esc undock» на станции: обещание, которое стареет молча. Легенда вынесена в константу `CHART_LEGEND` и связана с привязками тестом. |
| 2026-09-15 | **Найдено там же: одна клавиша на два действия.** `fire` и `launchMissile` оба несли **KeyM** — наследство оригинального Elite, где M *и была* клавишей ракеты. Когда огонь переехал на M, а ракета на N, старый алиас остался. Так как `fire` читается через `held` (не расходует клавишу), а ракета через `consume`, срабатывало **и то и другое**: замер с захваченной целью — одно нажатие M снимало ракету (3 → 2) **и** выпускало трассер. Основная клавиша огня тихо расходовала ракеты, которых на корабле единицы. Алиас удалён. |
| 2026-09-15 | Замер после правки: **M — ракеты 3 → 3, трассер есть** (только огонь); **N — ракеты 3 → 2, трассера нет** (только ракета). Легенда карты теперь «O = close   H = hyperspace   arrows/WASD = move cursor   T/G = step». |
| 2026-09-15 | **Тест «одна клавиша — одно действие».** `consume` отдаёт клавишу тому, кто спросил первым, и удаляет её, поэтому пересечение привязок всегда молчит: либо одно действие не происходит, либо происходят оба. Ни то ни другое не является чьим-то решением. Тест обходит всю таблицу и требует, чтобы каждый физический код принадлежал ровно одному действию — после снятия алиаса это единственное пересечение в таблице. |
| 2026-09-15 | **Тест «HUD не обещает клавишу, которая ничего не делает»** проверяет обе подсказки (легенду карты и экран заголовка) против `BINDINGS`: и отдельные буквы, и названные слова (ENTER, ESC, SPACE, TAB). Проверено, что он **падает** на старой легенде и проходит на новой, — иначе это был бы тест, который не может упасть. Легенды вынесены в константы `CHART_LEGEND` и `TITLE_CONTROLS`, чтобы их можно было читать из теста. |
| 2026-09-15 | Тесты: `input.test.js` (+2: одна клавиша — одно действие; клавиша огня не запускает ракеты), `hud.test.js` (+1: HUD не обещает несуществующих клавиш). Итого 618 юнитов. |
| 2026-09-15 | **Найдено на снимке экрана станции: подсказка называла не ту клавишу.** Внизу было написано «**D undock**», но `dock` привязан к **KeyC**, а **KeyD — это roll-right**, который на экране станции двигает выделение вниз. То есть подсказка предлагала нажать клавишу, которая прокручивает список, из которого игрок пытается выйти. Настоящая клавиша, C, в подсказке не упоминалась вовсе. |
| 2026-09-15 | **И вторая клавиша из той же подсказки не была привязана ни к чему.** «Esc undock» обещал Escape, а в `BINDINGS` не было ни одного `Escape` — самая вероятная первая проба новичка не делала ничего. Escape привязан отдельным действием `leave` (не алиасом `dock`: `dock` в полёте означает «запросить стыковку», а на станции «уйти со станции», и одна привязка не может значить и то и другое). `leave` теперь работает и на станции, и на карте. |
| 2026-09-15 | **Найдено там же: список рынка не следовал за выделением.** Замер: 19 строк, **724 px содержимого в 569 px окна**, последняя строка **на 45 px ниже среза**, и `scrollTop` после обхода всего списка оставался **нулём**. Стрелками можно было выбрать товар и не увидеть его — способ купить не то и не узнать. Правило вынесено в чистую функцию `revealScrollTop`, DOM-обвязка вокруг неё — три строки. |
| 2026-09-15 | Замер после правки: обход всех 19 строк — **0 шагов с выделением вне экрана**, `scrollTop` 0 → 44. Escape: `docked → flight`. D: остаётся `docked`, как и говорят привязки. Подсказка теперь читается «C undock Esc undock». |
| 2026-09-15 | **Тест связал слова с привязками.** «Каждая клавиша, которую называет подсказка, действительно привязана» проверяет и текст подсказки, и `BINDINGS` — включая обратную сторону: если `dock` когда-нибудь переедет на KeyD, тест потребует вернуть D в подсказку. Без этого слова и привязки расходятся молча, как уже разошлись. |
| 2026-09-15 | Своя ошибка по ходу правки: заменил строчный комментарий `// --- Small DOM helpers ---` на `/** ...` без закрывающего `*/`, и всё после него (включая `el`) уехало в комментарий — 48 тестов файла упали с «el is not defined». Поймано сразу, но это класс «правка комментария ломает код». |
| 2026-09-15 | Тесты: `station.test.js` (+6) — правило прокрутки (строка в окне не двигает список; ниже среза — доводит ровно на столько; выше — возвращает; никогда не уходит выше нуля; без измеренной высоты окна ничего не делает) и связанность подсказки с привязками. Итого 615 юнитов. |
| 2026-09-15 | **Найдено на снимке: журнал сообщений нечитаем.** HUD рисуется прямо поверх сцены, без своей подложки, поэтому строка над станцией оказывалась тёмной по светлому. Замер по кадру с HUD и без него: **средний коэффициент контраста 1.74**, **97 % пикселей журнала ниже порога читаемости 3:1**. |
| 2026-09-15 | **Причина — двойная прозрачность.** Цвет `inkDim` сам несёт альфу 0.38, а рампе журнала её ещё раз умножала: самая старая строка выходила на **0.13** эффективной альфы — нечитаемо даже на пустом космосе. Исправлено: `inkDim` поднят до 0.62, пол рампы — с 0.35 до 0.78, и каждая строка получила **тёмную обводку глифов**, которая едет вместе с буквой и держит контраст на любом фоне. Обводка рисуется на полной силе, гаснет только сама буква. |
| 2026-09-15 | Замер после правки: ядро глифа **0.065 → 0.1179**, фон под текстом **0.0000** (обводка), контраст **3.36:1**. То же лечение получили аварийные предупреждения («MISSILE», «HULL CRITICAL»): они висят по центру кадра, то есть ровно там, где станция или звезда занимает весь экран. |
| 2026-09-15 | **Тест поймал ошибку в самой правке.** Обводка рисовалась *до* установки `globalAlpha`, поэтому наследовала альфу предыдущей строки: у второй строки журнала обводка была 0.78 вместо 1.0. Тест «обводка рисуется на полной силе» это увидел. Побочно пришлось поправить существующий тест «журнал не рисует больше своего лимита»: он считал вызовы текста, а их стало вдвое больше — теперь считает различные строки, что и было его смыслом. |
| 2026-09-15 | Тесты: `hud.test.js` (+4) — каждая строка журнала обведена до заливки; обводка на полной силе независимо от альфы строки; самая тусклая строка композитится не ниже 3:1 к чёрному (чистая арифметика по палитре и рампе, без канваса); аварийные предупреждения обведены тоже. Итого 609 юнитов. |
| 2026-09-15 | **Найдено глазами и подтверждено замером: заливка была слишком тёмной, и обводка снова стала всей картинкой.** Ради этого затухание по глубине и вводилось («обводка — акцент, силуэт несёт залитый корпус»), но несущей оказалась именно обводка. Замер по пикселям самого объекта (кадр с ним минус кадр без него): у пирата в 500 юнитах средняя яркость **18.2**, **89 % пикселей темнее 35**, выше 110 — только 4 %. В кадре это белый контур, внутри которого ничего нет. Пояс — поле проволочных шаров. |
| 2026-09-15 | **Причина оказалась в несогласованности, а не в балансе.** У корпусов `envMapIntensity` 1.35, у станции 1.5, а у камней — `0.5 + metalness`. Замер с выключенной пробой окружения: камень падает с 14.4 до **1.2**, то есть пробу окружения и делает почти всю видимую работу, а камни отдавали половину её. Камни просто не получили той обработки, которую когда-то получили корпуса. |
| 2026-09-15 | Исправлено: альбедо камней и корпусов поднято в **2.2 раза по линейной яркости** (в байтах это ≈1.5 раза, отсюда новые hex). Замер A/B на одной и той же позе: у пирата доля пикселей выше 35 выросла **15.8 % → 26.5 %**, у канистры 13.8 % → 20.5 %. Типы камней стали различимы на дистанции: на снимке пояса видно синий лёд, медный железняк и серый гранит, тогда как раньше это были одинаковые белые сетки. |
| 2026-09-15 | **Обводка scenery отступает раньше, чем обводка целей.** У корабля в 500 юнитах — сорок пикселей, и контур там *и есть* силуэт; у камня нет. Поэтому у камней своя полоса затухания (`ROCK_EDGE_FADE`, 150→1200 вместо 260→3400): прозрачность ~0.86 в 300 юнитах, ~0.60 в 600, ~0.38 в 800 и 0.14 за 1200. Полоса кончается там, где начинается пояс, — в этом и смысл. |
| 2026-09-15 | Тесты: `geometry.test.js` (+3) — камень получает ту же обработку окружением, что и корпус (и не пересвечивает станцию); обводка scenery отступает раньше обводки целей (проверяется через `material.userData.fade`, потому что юниформы существуют только после компиляции шейдера); самая тусклая порода не темнее порога, ниже которого заливка читается как контур. Итого 605 юнитов. |
| 2026-09-15 | **Замер по ходу работы трижды врал, и это тоже результат.** (1) Первый замер сравнивал среднюю яркость фиксированной области экрана — то есть измерял *размер* объекта, а не его яркость, и маленький тёмный корабль выглядел как другая проблема, чем большой тёмный камень. (2) Направление камеры было посчитано неверно (`p - off` вместо `p - pos`), и проба мерила пустой космос: множитель альбедо «не влиял» вообще. (3) Субъекты двигались между двумя кадрами, из которых берётся разность, — ИИ уводил их из кадра, и замер зависел от того, куда они улетели. Все три починены в стенде, а не в игре. |
| 2026-09-15 | **Охват: написан, измерен и откачен.** Открытая проблема «пираты не координируются в охват» предполагала, что пара атакующих сходится на одном пеленге и очередь стоит друг за другом. Стенд показал обратное: средний угол между двумя пиратами, пока оба в зоне огня, — 83° при старте с 700 юнитов, 76° с 1100, 90° с 1600, 120° с 2400, 122° с 3400. Пираты и так заходят с разных сторон. Реализованный охват (каждому фланг вдоль поперечной оси командира) менял эти числа на 0–3°, а на 1600 даже ухудшал (90 → 78). Код откачен, `PINCER` и `flankAim` удалены. |
| 2026-09-15 | **Почему охват не может работать в этой модели движения.** Скорость корабля следует за его носом (`integrateEntity`), а стрельба требует, чтобы нос смотрел на цель в пределах 10° (`cone > 0.985`). Значит, лететь по фланговой траектории и целиться одновременно невозможно: каждый юнит фланга внутри зоны огня — это отданный выстрел. Замер подтвердил: при вводе в бой с 200 юнитов — 2 выстрела за 30 с, с 300 — **0**, с 480 — 5, и только на 620 (граница огня) — 11 против 12 у прежнего поведения. |
| 2026-09-15 | **Стенд сам породил «баг», который я собрался чинить.** Первый замер дал 48° → 165° и выглядел как победа. Причина оказалась в стенде: `spawn` задаёт кораблю скорость вдоль **старого** носа, и она рассасывается около секунды — ровно столько, сколько длился замер. Обнулив скорость, я получил 74° у контроля против 76° у охвата, то есть ничего. Урок: прежде чем радоваться «до/после», убедись, что оба прогона начинаются из одинакового чистого состояния. |
| 2026-09-15 | Побочный, но полезный результат: голое число `620` в условии стрельбы стало `C.FIRE_RANGE` в `combat.js` — это боевая величина, и она заслуживает имени рядом с остальными. Тест пинит поведение к константе с обеих сторон: за границей не стреляют, внутри — стреляют (без второй половины тест прошёл бы и на пушке, которая не стреляет никогда). |
| 2026-09-15 | **Взятая зачистка теперь меняет саму систему.** Контракт на зачистку — единственный без пункта назначения: он выписан в той же системе, где висит на доске (`target: system`, `distance: 0`). До правки его взятие не меняло ровным счётом ничего — игрок брал обязательство «убить шесть пиратов здесь» и ждал, пока нужные корабли случайно залетят. Теперь система становится **укреплённым гнездом**: свои враждебные корабли поверх обычного трафика, во внутренней части оболочки спавна (420–760 вместо 420–1100), пополняются пока контракт открыт. |
| 2026-09-15 | **Найдена и исправлена ошибка: контрактов на зачистку не существовало ни одного.** Блок `bounty` в `generateBoard` стоял последним и срабатывал только `if (offers.length < boardSize)` — а цикл доставок заполняет все три слота всякий раз, когда система производит три вещи, нужные соседям. Замер: **0 зачисток на 320 досок**, 210 из них были заполнены грузовыми строками. Один из четырёх типов контрактов был мёртвым контентом, а вместе с ним — и всё гнездо. Теперь слот под зачистку резервируется заранее: зачисток 210 из 320, «только груз» 73 % → 27 %. |
| 2026-09-15 | **Найдены и исправлены две утечки в `enterSystem`.** (1) Корабли трафика живут в сцене рендерера, а не в группе системы, поэтому каждый прыжок оставлял флот прошлой системы в сцене навсегда: замер — **104 замороженных корабля после двенадцати прыжков** при десяти в текущей системе. Они стояли ровно там, где положено трафику (420–1100 от станции), то есть читались как «корабли перестали двигаться», а не как утечка. (2) Свет добавлялся заново на каждом прыжке и не убирался: **21 направленный свет после двадцати прыжков**, и звезда покинутой системы продолжала освещать новую. Утечки нашёл новый e2e-раздел, который прогоняет `enterSystem` десятки раз, — прежние 51 проверка делали один прыжок и не видели этого. |
| 2026-09-15 | **Асимметрии у гнезда нет — и это замер, а не лень.** Соблазн был добавить «в гнезде пираты злее», но враждебные и так рождаются с агрессией 0.55+ при пороге драки 0.5, то есть дерутся всегда. Крутилка, которая не меняет картинку, хуже её отсутствия: она читается как механика. Тот же класс, что `fleeing` и невызванный модуль репутации. |
| 2026-09-15 | Гнездо считается по **своим** кораблям (поле `pocket`), а не по враждебным вообще: первая версия давала контракт на 4 → **7** враждебных в системе с danger 0, потому что свой пират съедал квоту, а потом обычная таблица добавляла сверху. Тест «система без контракта строится ровно как раньше» пинит это побайтово — позиции и виды совпадают до шестого знака, то есть новый путь не сдвигает поток случайных чисел. |
| 2026-09-15 | Тесты: `missions.test.js` (+13: счёт долга, согласие с `bountyProgress`, достижимость зачистки, спокойная система без зачистки, одинокий мир, разнообразие доски), `world.test.js` (+10: гнездо, его размер, пол, пополнение, закрытие, кламп, радиус, утилизация трафика), e2e (+5: взять контракт через экран станции, увидеть гнездо, зачистить, увидеть, что оно закрылось). Итого 601 юнит + 56 e2e. |
| 2026-09-15 | **У пиратов появились ракеты.** До этого ракеты были односторонним оружием: у игрока с самого начала, у NPC — никогда. Это единственная атака, на которую надо **отвечать**, а не терпеть, и без неё бой сводился к размену уроном. |
| 2026-09-15 | Ракета слабее и менее вёрткая, чем у игрока (34 урона против 78, поворот 1.3 против 2.1, скорость 430 против 620). Асимметрия — это и есть замысел: ракета, которая доворачивает так же, как ваша, — не угроза, а подбрасывание монеты. При 1.3 рад/с против корабля, который может её перекрутить, манёвр работает. |
| 2026-09-15 | Ответ на ракету не только «убежать»: **её можно сбить лазером**. Луч ищет по объединённому списку кораблей и ракет, поэтому ближайшая цель побеждает — а входящая ракета почти всегда ближе, чем тот, кто её пустил. Отдельное сообщение «Missile destroyed», потому что это единственный выстрел в игре, который спасает корабль, а не уничтожает что-то. |
| 2026-09-15 | Запуск рацион: только у пиратов (1) и рейдеров (2), только в окне 260–760 юнитов, только с захватом > 0.93, только не в разрыве боя, с перезарядкой 9–18 с. Торговцы не носят ракет вообще — «грузовик, стреляющий в ответ» это другая игра. |
| 2026-09-15 | Проверено в браузере: пират пускает, HUD показывает «MISSILE INBOUND», ракета снимает ровно 34 щита, и лазер её сбивает с сообщением «Missile destroyed». Тесты: `world.test.js` (+12) на пуск, магазин, перезарядку, ближнюю и дальнюю дистанцию, захват, разрыв боя, торговцев, асимметрию характеристик. Итого 573/573. |
| 2026-09-15 | **Пояс перестал быть одним камнем, размноженным девяносто раз.** Форма уже гуляла — у каждой вершины своё радиальное смещение, — но *характер* был один: тот же икосаэдр, те же две цветовые константы. Теперь четыре породы: базальт (38 %), гранит (26 %), железняк (24 %), лёд (12 %). Различаются числом граней (икосаэдр с подразбиением, без него, додекаэдр), цветом и материалом — лёд глянцевый, руда металлическая. Плюс разброс оттенка на каждый камень. |
| 2026-09-15 | **Тесты поймали три ошибки, каждая поучительна.** (1) Выбор породы одной ступенью ЛЦГ кластеризовался: из четырёх типов достигались два — один шаг ЛЦГ не хеш, взял `mulberry32` из проекта. (2) Первый набор цветов дал гранит и лёд почти одного цвета (зазор 0.0164 против разброса оттенка 0.0171) — камень одного типа мог выглядеть как другой. (3) Моя метрика «различимости» суммировала каналы RGB и объявила серый и синий одинаковыми; заменил на евклидово расстояние. |
| 2026-09-15 | Тесты: `geometry.test.js` (+11) — все четыре породы достижимы, разное число граней, различимые цвета, детерминизм, разброс оттенка **уже** зазора между породами, взвешенная доля с точностью 3 %, лёд глянцевый и руда металлическая, пояс строится из разных пород. Итого 561/561. |
| 2026-09-15 | **Космическая пыль — чувство скорости вернулось.** После удаления выхлопа выяснилось, что в открытом космосе **нечем мерить скорость**: звёзды бесконечно далеко и не двигаются, поэтому 90 единиц в секунду и покой выглядят одинаково. Новый `src/sim/dust.js` — 170 пылинок в оболочке 14–60 юнитов вокруг корабля, которые не двигаются, а корабль летит мимо них. Настоящий параллакс: ближние проносятся быстро, дальние медленно. Выпавшие за оболочку переносятся вперёд. Так было и в оригинале 1984 года. |
| 2026-09-15 | Пыль измерена, а не «поставлена и забыта»: **0.07 % кадра** против 55–97 % у удалённого выхлопа. Внутренний радиус оболочки — 14 юнитов, чтобы ни одна пылинка не оказалась вплотную к объективу (ровно та ошибка, из-за которой выхлоп пришлось убрать). |
| 2026-09-15 | Тесты: `dust.test.js` (17) — правило рециркуляции как чистая арифметика, размещение относительно корабля, а не начала координат, смещение вперёд с сохранением широкого веера (замер: 2.8 % позади, средний перпендикуляр 0.70), облако следует за кораблём на 400 шагов, тусклость и отсутствие записи глубины. Итого 550/550. |
| 2026-09-15 | **Экономика измерена, а не угадана.** Новый `scripts/economy-sim.mjs` прогоняет жадного оптимального торговца (знает все цены в радиусе прыжка) через сотни прыжков на настоящих модулях. Результат: рост **затухает** — 92 % за прыжок в начале, 1.23 % к 120-му, 0.36 % к 400-му, потому что трюм фиксирован и прибыль за рейс упирается в него. Открытая проблема «экономика не откалибрована» была **гипотезой**; замер её не подтвердил. |
| 2026-09-15 | **У пиратов появилась мораль.** Ветка `fleeing` в машине состояний существовала с самого начала, и **флаг никто не выставлял** — то есть раненый пират с двумя хитами летел в пушки до конца. Теперь при остатке корпуса ниже 30 % корабль разрывает бой и уходит от игрока, и больше не стреляет. |
| 2026-09-15 | **Стайный отклик.** Три пирата в радиусе дрались с игроком по очереди, пока двое летали по патрулю. Теперь идущий бой в радиусе 620 юнитов втягивает остальных рейдеров. Торговцев не втягивает никогда, убегающий лидером не считается, радиус подобран так, чтобы не превращать игру в побоище. |
| 2026-09-15 | Проверено в браузере: до драки все трое `patrol`, после того как один открыл огонь — все трое `engage`, после урона лидер `flee`. Тесты: `world.test.js` (+9) на мораль, побег, прекращение огня, стаю, радиус и исключения. Итого 532/532, e2e 51/51. |
| 2026-09-14 | **Найдена и исправлена ошибка, из-за которой корабль летел спиной вперёд.** Камера брала кватернион корабля как есть, а камера смотрит по своей локальной **−Z**, то есть по **хвосту**. Замер: после отстыковки станция впереди на 376 юнитах (`stationAhead = 1`), две секунды полного газа — **799**. Игрок смотрел, как то, на что он смотрит, улетает. HUD при этом уже был написан под правильную камеру: его базис проекции — нос корабля, так что рамка цели и трассеры рисовались в вид, которого камера не показывала. Камера теперь доворачивается на пол-оборота внутри системы корабля (`CAMERA_FLIP`). |
| 2026-09-14 | **Выхлоп игрока удалён.** Он был либо невидим, либо заливал кадр: частицы рождались в 3–17 юнитах от объектива (глаз стоит в 7.4 позади точки отсчёта корабля), а затухание бралось по самой старой частице непрерывного потока — то есть всегда по умирающей. Замер: 38 частиц, все перед камерой, 55–97 % кадра. В такой геометрии красивого варианта не существует; подробности в комментарии на месте удалённого кода. |
| 2026-09-14 | **HUD масштабируется по высоте окна.** Он смешивал пропорциональные размеры (радиус сканера — доля экрана) с фиксированными (13px текст, 8px полоса), поэтому на 1080p текст оставался прежним, пока всё вокруг росло. `hudScale(height)` с зажимом 0.9…1.5; применяется и к экрану заголовка. |
| 2026-09-14 | Мигающая подсказка «PRESS M TO LAUNCH» показывается 77 % времени вместо 50 %: при равном цикле треть игроков смотрит в тёмную половину и не видит, что нажимать. |
| 2026-09-14 | Тесты: `render.test.js` (+1, камера смотрит по носу и пол-оборота вокруг **своей** оси, а не мировой), `hud.test.js` (+5, масштаб, зажим, шрифт на 1080p больше, заголовок масштабируется, подсказка видна больше половины цикла). Итого 523/523, e2e 51/51. |
| 2026-09-13 | **Контракты.** Новый модуль `src/logic/missions.js` — доска заданий на станции. Четыре типа: доставка (везёшь то, чего не хватает соседу), гуманитарный рейс (то же в систему в кризисе, платит вдвое и двигает репутацию на 14), курьер (документы, ноль тонн, срок короткий, платят за дальность) и зачистка пиратов (единственный контракт, который считается по памяти мира, а не по трюму). |
| 2026-09-13 | Генерация выводится из того, что галактика уже знает: система предлагает то, что сама производит, а цель — тот сосед, которому это нужно (`profile.consumes`, `profile.lacking`, `condition.demand`). Награда складывается из ценности груза, дальности и опасности направления. |
| 2026-09-13 | **Найдена и исправлена ошибка в дизайне доски.** Первая версия *предпочитала* цель в кризисе, и доска выходила сплошь «emergency». Причина не в том, что кризисов много (20 % систем), а в том, что кризис — единственное, что заставляет систему хотеть товар, который она обычно не потребляет. Замер на Lave: пул нуждающихся в еде — 12 спокойных и 5 в кризисе, а в скоте и медикаментах — 0 спокойных и 4–6 в кризисе. Два ряда из трёх были вынужденно гуманитарными. Теперь не больше одного рейса на доску. |
| 2026-09-13 | Вкладка **Contracts** на станции: предложения и принятые контракты одним списком, `Enter` берёт или отказывается, `1` по-прежнему тонна на рынке. В HUD — предупреждение только когда срок истекает завтра, а не постоянный счётчик. |
| 2026-09-13 | Полный цикл проверен в браузере через настоящий интерфейс: принял гуманитарный рейс (15 т еды), довёз — контракт закрыт, **+521 CR**, **+14 репутации**, груз списан. Просрочка: контракт снят, **−100 CR**, **−9 репутации**, счёт не ушёл в минус. |
| 2026-09-13 | Тесты: `missions.test.js` (39) — детерминизм доски, уникальность id, что доставка везёт то, что система производит, а цель это потребляет, что рейс в кризис действительно в кризис, что награда растёт с дальностью, что контракт нельзя выполнить дважды и нельзя выполнить без груза, что базовая линия зачистки не считает старые убийства, что просрочка стоит денег и репутации и не уводит счёт в минус. Плюс 11 в `station.test.js` на вкладку. Итого 517/517, e2e 51/51. |
| 2026-09-13 | **Репутация перестала быть декорацией.** `standingPriceFactor` применяется к закупке в `marketRows` (Allied −6 %, Hunted +15 %); `STANDING_TIERS[].greeting` показывается при входе в систему; `patrolHostile` делает патрули враждебными на входе. Все три функции существовали с самого начала и не вызывались ни разу. |
| 2026-09-13 | **Память мира ожила.** `createMemory` создавалась заново в `enterSystem` при каждом прибытии, поэтому накапливаться ничего не могло, а `rememberEvent` вообще не имел вызывающих. Память переехала на запись игрока (`player.systemMemory`), сохраняется вместе с ней, наполняется событиями (убил пирата/патруль/торговца, привёз еду голодающим, прилетел) и влияет на опасность системы, плотность трафика и цены. |
| 2026-09-13 | Найден и починен **баг в слухах**: подборщик строк был написан как `(i) => name.charCodeAt(i % name.length)`, то есть получал массив строк, игнорировал его и возвращал код символа. Слух при входе в систему печатался числом (`76`) вместо фразы. Добавлен `rumourPicker(seed)`. |
| 2026-09-13 | **Частичная торговля.** `doBuy` всегда брал максимум, `activate()` продавал весь груз — нельзя было ни отложить деньги на топливо, ни выпутаться из «полный трюм + пустой бак». Клавиша `1` торгует ровно одну тонну, `Enter` — всё, как раньше. |
| 2026-09-13 | **Индикатор направления урона.** Раньше при попадании сзади была только строка текста, и только для кормового сектора. Теперь `noteHit` считает пеленг в системе корабля, а HUD рисует дугу у края экрана, гаснущую за 1.4 с; столкновения тоже дают пеленг. |
| 2026-09-13 | Тесты: `reputation.test.js` (+15, память мира и подборщик слухов), `world.test.js` (+9, реакция системы на историю и враждебные патрули), `station.test.js` (+6, частичная торговля), `hud.test.js` (+7, дуги урона). Итого 465/465, e2e 51/51. |
| 2026-09-13 | **Рефакторинг по итогам code review.** Закрыто 7 дефектов: `decayWanted` вызывалась без индекса системы (розыск не истекал никогда), `payFine` искала `standing` по индексу системы вместо фракции, Docking Computer не читался нигде, `neighbors` получал световые годы вместо сырых единиц, `prune()` не освобождал геометрию, `session.avgPaid` не сохранялся, `isIllegal` вызывалась с неверной арностью. |
| 2026-09-13 | **Найдена третья лестница репутации.** `PLAYER.standingLabel` и `REP.tierFor` — две независимые кодировки одной шкалы, расходившиеся ровно на трёх границах (−60, −25, +8): командир с +8 видел «Neutral», а рынок давал ему скидку «Liked». В `station.js` уже лежала заметка `techdebt:` с предписанием убрать дублирование; её посылка «сегодня они совпадают» была неверна. Одна лестница, `tierFor` — источник истины. |
| 2026-09-13 | **Небо: 534 меша → 2.** Туманности и пыль были по отдельному `Mesh` со своей `PlaneGeometry` на каждый билборд — 78 % всех draw-call'ов игры на статичный фон. Теперь два `InstancedMesh`. Мешей в сцене 680 → 148, уникальных геометрий 769 → 236. |
| 2026-09-13 | **Прозрачность и `instanceColor` живут в разных пространствах.** `material.opacity` умножает уже закодированный sRGB, а instance-цвет — линейный. При `o = 0.09` расхождение в 2.7 раза (байт 7 против 19). Прозрачность свёрнута через `convertLinearToSRGB` → `multiplyScalar` → `convertSRGBToLinear`; сверено с прежней реализацией на пяти значениях, совпадение побайтовое. |
| 2026-09-13 | **Доказано отсутствие визуальной регрессии.** Старая и новая реализации неба отрисованы изолированно, с общей камерой и текстурой: пыль — 0 различающихся пикселей, туманности — максимум 1 (округление). Контрольный прогон: две одинаковые сборки дают 8.4 % различий, «до/после» — 3.2 %, то есть рефакторинг ближе к базовой линии, чем сборка к самой себе. |
| 2026-09-13 | **Выхлоп переведён на пул.** 90 аллокаций `THREE.Mesh` и 180 операций с графом сцены в секунду → один `InstancedMesh` на 192 частицы. Заодно найдено, что общее затухание по самому старому частице (верное для обломков, которые рождаются вместе) для непрерывного потока выхлопа держало `opacity` на 0.004 — след был невидим всегда. Затухание стало почастичным. |
| 2026-09-13 | **Выхлоп: починка найдена, измерена и откачена.** Почастичное затухание в `instanceColor` делает след видимым — и он накрывает 65 % кадра белой пеленой, потому что частицы проходят в единице от камеры. Откачено к прежнему поведению: рефакторинг не должен менять картинку. Обе проблемы (мигающее затухание и размещение) записаны в открытые. |
| 2026-09-13 | Гигиена: убраны 4 неиспользуемых импорта, `session.chartTarget` (только читалось), `entity.lod` с обещанием несуществующего LOD, `player.hold`, заглушки `void`, `accumulator`. Дублирующийся `disposeTree` вынесен в `sim/dispose.js` и теперь соблюдает правило `userData.shared`, которое комментарии уже описывали, а реализация игнорировала. |
| 2026-09-13 | Тесты: `reputation.test.js` (21), `player.test.js` (29), `dispose.test.js` (9); обновлены `sky.test.js` под instanced-структуру и `station.test.js`. Итого 428/428, e2e 51/51. |
| 2026-09-13 | Тест «оплата штрафа повышает репутацию» создавал `standing[0]` — запись по индексу системы, которой у настоящего игрока не бывает. Тест и баг согласились друг с другом. Переписан на реальный ключ фракции. |
| 2026-09-12 | **Осовременивание графики, шаг 4: пламя двигателя, выхлоп, обломки.** `makeFlame()` — конус `BackSide` + `AdditiveBlending` (дальняя стенка видна сквозь ближнюю, две складываются в горячее ядро одним мешем). Все четыре фабрики кораблей идут через `shipShell()`. |
| 2026-09-12 | Сопло садится на **собственную заднюю вершину корпуса** (`hullTailZ`), а не на константу: корпуса разного масштаба (Cobra хвост −2.4, trader −4.2), общий офсет стрелял бы из кабины на одном из них. Пламя = 27.7% длины корпуса при полном газе. |
| 2026-09-12 | Пламя ведём от **угловой скорости**, не от газа — у NPC нет троттла. См. ADR. `syncFlame` в `world.js`, сглаживание `6 * dt`, порог 1.6 рад/с. |
| 2026-09-12 | **Тени решено не делать** — ADR «тени не рисуем». Единственный источник — звезда, корпуса — открытые оболочки, принимать тень нечему. Вместо теней объём даёт PBR-отклик + фасеточное затенение. |
| 2026-09-12 | Выхлоп в полёте (`stepExhaust`, только при `thrust > 0.25`) и обломки при взрыве (`spawnDebris`, общие геометрия и материал на все осколки). Очистка пула в `enterSystem` и `dock()`. |
| 2026-09-12 | Тесты пламени: 6 в `world.test.js` (наличие и материал, посадка на хвост, покой не ноль, реакция на вираж, потолок газа, отсутствие у камней/канистр). Проба на вираж держит игрока **вблизи** — `steerToward` даёт тем меньшую угловую скорость, чем дальше цель. Итого 355/355, e2e 51/51. |
| 2026-09-12 | **Ложная тревога, разобранная до конца:** большая голубая «гексагональная» масса в кадре с кораблями оказалась не пламенем, а билбордом туманности неба (`PlaneGeometry` в тысячи юнитов, opacity ~0.07). Проверено скрытием пламени — фигура осталась. Пламя при этом корректно: конус, `BackSide`, 31 юнит против 113-юнитового корпуса Viper. |
| 2026-09-12 | **Осовременивание графики, шаг 3: PBR-материалы и проба окружения.** `MeshLambertMaterial` заменён на `MeshStandardMaterial` во всех корпусах. Новый модуль `src/sim/environment.js` — процедурная equirect-проба (`DataTexture`, `LinearSRGBColorSpace`, `FloatType`): вертикальный градиент зенит/надир, галактическая полоса по тому же наклону, что в `sky.js`, 7 туманностных пятен, блик звезды. Проба крепится к `scene.environment`; без неё металл рендерился бы чёрным. |
| 2026-09-12 | Разные поверхности получили разный отклик: корпуса `metalness 0.55 / roughness 0.42`, станция — самая полированная (`0.68 / 0.34`), астероиды — матовый камень (`0 / 0.95`), канистры — крашеный металл (`0.6 / 0.38`). Контраст «матовый камень против глянцевого металла» разделяет пояс и флот без разглядывания обводки. |
| 2026-09-12 | Обводка стала акцентом, а не единственным носителем силуэта: `onBeforeCompile` в `edgeLines` даёт затухание по глубине вида (smoothstep, band 260→3400, floor 0.14). Ближний бой сохраняет полную яркость, станция на 500–1400 юнитах больше не читается как леса вокруг полированного корпуса. |
| 2026-09-12 | `AmbientLight` 0.7→0.45, `HemisphereLight` 0.55→0.38: непрямой свет теперь даёт проба, старые значения выравнивали те градиенты, ради которых она существует. |
| 2026-09-12 | **Поймана ошибка компиляции GLSL, которую не видит юнит-тест:** патч `begin_vertex` объявлял `vec4 mvPosition`, а three объявляет его же в `project_vertex` ниже в том же шейдере — «redefinition», 50/51 e2e. Переименовано в `fadeViewPos`; в `geometry.test.js` добавлен запрет на это имя (с вырезанием комментариев — в комментарии оно упомянуто намеренно). |
| 2026-09-12 | Тесты: `environment.test.js` (11), материалы и затухание в `geometry.test.js` (+6), фасад пробы и «приглушённый свет» в `render.test.js` (+2). Итого 349/349, e2e 51/51. |
| 2026-09-11 | **Осовременивание графики, шаг 1: процедурное небо.** Новый модуль `src/sim/sky.js` — 9000 звёзд с галактической полосой (Box-Muller по нормали диска), 14 эмиссионных туманностей по спектральным линиям (H-alpha, OIII, отражённые), 520 пылевых билбордов. Одна общая glow-текстура на canvas. 12 тестов. |
| 2026-09-11 | `rng.js`: добавлены `noise3`, `fbm3`, `ridged3` для будущего рельефа планет. 9 тестов. |
| 2026-09-11 | **CRT-пасс удалён** (scanline/vignette/aberration/grain). С `PBR`-материалами и живым небом оверлей мешал картинке, а не обрамлял её. Цепочка теперь `RenderPass → bloom → OutputPass`. `LOOK.crt` больше нет. |
| 2026-09-11 | **Три бага, найденных скриншотом, а не логикой:** (1) туманности с `depthTest:false` рисовались поверх станции — синяя заливка внутри корпуса; (2) `DoubleSide` на корпусе станции давал «стеклянный шар» — закрытые тела теперь `FrontSide`, открытые корабли остаются `DoubleSide`; (3) `polygonOffset` на корпусе тянул его назад, и дальние рёбра станции проходили сквозь ближние — офсет переехал на линии с минимальным шагом. |
| 2026-09-11 | `HemisphereLight` (холодный зенит / тёплый низ) — металл больше не читается плоским серым при крене. |
| 2026-09-11 | Тесты: `render.test.js` переписан под группу неба (7 новых), `sky.test.js` фиксит тавтологичное `depthTest`-утверждение, `geometry.test.js` пинит новый инвариант офсета. Итого 317/317, e2e 51/51. |
| 2026-09-11 | **Починен чёрный 3D.** Причина — `Quaternion.slerp` в three r186 читает приватные `_x/_y/_z/_w`, а `flight.quat` это плоский литерал; slerp молча возвращал NaN, камера травила `matrixWorld`, кадр чёрный без единой ошибки. Добавлен `toQuaternion()` в `render.js`. |
| 2026-09-11 | 4 теста на этот класс отказа: пинит поведение three + проверяет по исходнику, что `slerp`/`applyQuaternion` не получают плоский кватернион. |
| 2026-09-11 | E2E: `protocolTimeout` 180→600 s, бэкенд `swiftshader`→`angle+swiftshader` (убрана флейка `CONTEXT_LOST_WEBGL`). |
| 2026-09-11 | `step(dt, time, { render: false })` — soak-тест 2000 кадров больше не рисует каждый кадр, прогон e2e ускорился в разы без потери покрытия. |
| 2026-09-11 | Освещение: свет звезды 1.55→2.35, ambient 0.55→0.7, `HULL_COLOUR` 0x1b2129→0x232b36. Корабли читаются как объём, сохраняя векторный силуэт. 3 теста на диапазоны. |
| 2026-09-11 | Документация: README, AGENTS.md, docs/CONTEXT.md. 3 новых ADR в DECISIONS.md. |
| 2026-09-11 | Удалены временные скрипты `tmp/` и устаревшие отладочные скриншоты из `e2e-shots/`. |
| 2026-09-11 | Сборка проверена: 671.54 kB / 179.34 kB gzip. E2E 51/51, юнит 298/298. |
| 2026-09-11 | Сборка после графики: 672.45 kB / 179.77 kB gzip. E2E 51/51, юнит 317/317. |

## Архив закрытых проблем

Строки, убранные из таблицы Open-проблем при разборе 2026-09-17. Здесь они
остаются как ответ на вопрос «а что с номером N» — но не как список задач.

| # | Priority | Описание |
|---|----------|----------|
| 1 | — | Закрыто: репозиторий создан локально (`git init -b main`, 64 файла, первый коммит). Пуш на GitHub — за пользователем: `gh` не авторизован, и это единственный шаг, который требует его учётных данных. |
| 2 | P3 | Закрыто масштабированием: межстрочные интервалы теперь кратны размеру шрифта, а не заданы в пикселях, поэтому запас сохраняется на любом разрешении. |
| 3 | P3 | Закрыто замером: `scripts/economy-sim.mjs` показывает затухающий рост (92 % → 0.36 % за прыжок). Скрипт остаётся в репозитории, чтобы перепроверять после правок баланса. |
| 4 | P3 | **Закрыто замером: координации в охват нет — и не требуется.** Замер двух пиратов, пущенных одним пеленгом: средний угол между ними, пока оба в зоне огня, — **83°, 76°, 90°, 120°, 122°** на дистанциях старта 700…3400. Пираты уже заходят с разных сторон; очередь, которую предполагала проблема, была артефактом стенда. Разделение на фланги меняло эти числа на 0–3° (11 против 10 выстрелов), поэтому код откачен. Подробности в ADR. |
| 5 | P3 | Звезда — 3 слоя аддитивных оболочек. Тени сознательно не рисуем — см. ADR «тени не рисуем» (нет пары «источник — приёмник»). |
| 6 | P3 | Закрыто: четыре породы камня с разным числом граней, цветом и материалом (см. журнал). |
| 7 | P3 | Закрыто: чувство скорости даёт космическая пыль (`dust.js`), а не выхлоп. Выхлоп в такой геометрии невозможен — он либо невидим, либо заливает кадр. |
| 9 | P3 | **Закрыто 2026-09-18: «торговля вдвое сильнее контрактов» было дефектом прибора, а не балансом.** Найдены и починены четыре дефекта `economy-sim.mjs`, все — в стенде, не в игре: часы тратили день на каждый прыжок (в игре прыжок бесплатен, день тикает на стыковке); зачистки не начинались, потому что симулятор искал соседа, «сокращающего расстояние» до цели в 0 ly; груз покупался один раз и не добирался; заказ, выполненный на месте, не сдавался, так как `resolveArrival` звался по следующей системе. После правок на 800 прыжках карьера с контрактами впервые обгоняет чистую торговлю — 76 085 против 69 516. Подробности — в «Журнал работ» за 2026-09-18. |
| 10 | P3 | Закрыто: зачистка делает целевую систему укреплённым гнездом — свои хосты поверх обычного трафика, ближе к станции, пополняются пока контракт открыт (см. журнал и ADR). |
| 12 | — | Закрыто: взятое предложение убрано из списка доступных. Задание видно один раз — живой строкой «in progress» с действием «abandon». Курсор по-прежнему один над одним списком: доступные предложения, затем взятые. |
| 14 | P1 | Закрыто: цели отбираются по **графу маршрутов** (`G.hopsBetween`, ≤ 6 прыжков), а не по прямой. Замер после правки: **0 недостижимых** из 1088 предложений. См. журнал и ADR «достижимость считается по графу маршрутов». |
| 15 | P2 | Закрыто: потолок тоннажа клампится `holdMaxOf` — `Math.min(maxTons, hold)`. Тест падал на прежнем поведении с **54 из 706** грузовых предложений, не влезающих в трюм. **Продолжение 2026-09-18:** тот же дефект был и на шаг раньше — заказ мог требовать больше, чем есть на складе системы-отправителя (**1387 из 3080, 45 %**, худший разрыв 18 т). Добавлен `suppliedStock`; вместе с ним закрыт и срок курьера, не влезавший в маршрут (Rari → Ores, 6 прыжков при 5 днях). См. журнал за 2026-09-18. |
| 16 | P2 | Закрыто раскрытием, а не сменой механики: покупка груза — задуманный путь (товар всегда из излишков системы, 145/145), но теперь об этом говорит и доска, и рынок. Добавлены `shoppingList` (что докупить, с учётом уже лежащего в трюме), пометка `NEEDED: N t` на строке рынка, панель «NEEDED FOR CONTRACTS» и явная строка на доске: «контракт не грузит трюм, а проверяет его». |

## Структура проекта

```
Elite-Deep/
├── README.md            # Точка входа
├── AGENTS.md            # Инструкции для агентов
├── index.html           # Оболочка: <div id="app"> + module-скрипт
├── vite.config.js       # Двухстадийная сборка, singlefile + классический тег
├── package.json
├── src/
│   ├── main.js          # Оркестратор: машина режимов, сессии, адаптеры
│   ├── logic/           # Чистая логика (rng, galaxy, economy, player, factions, combat, rep)
│   ├── sim/             # Мост к three (models, world, flight, render, input, audio)
│   └── ui/              # hud.js (Canvas), station.js (DOM)
├── tests/               # node:test, зеркалит src/
├── scripts/e2e.mjs      # puppeteer, гоняет настоящую игру
├── docs/                # CONTEXT.md, DECISIONS.md
└── e2e-shots/           # Скриншоты (перезаписываются прогоном e2e)
```
