# HANDOFF — Elite-Deep

> Создан: 2026-09-19 18:15 UTC
> Причина: переезд в новую сессию по просьбе владельца (длинная сессия: ревью → правки → CI-битвы → баланс)

## Текущая задача

Активной задачи нет. Ждём живого теста игры владельцем (#13) и его решений по #8 и #11. Состояние зелёное везде.

## Что сделано в этой сессии

- Проверка синхронизации с GitHub: чисто, 0 расхождений.
- Первое ревью (3 блокера) → все закрыты и проверены; файл `docs/CODE-REVIEW-2026-09-19.md`.
- Второе ревью (3 новых блокера) → все закрыты; файл `docs/CODE-REVIEW-2026-09-19-followup.md`.
- Файлы ревью прибраны: оглавление всех ревизий в `docs/CODE-REVIEW.md`, шапки ужаты, тела-снимки не тронуты.
- CI был красным 4 раза подряд — все причины найдены и починены (кулдаун стеной против игровых часов; валидатор против `lastVisitDay: null` и дробного `_offences`; парковка мимо сферы 0.62R; голый keydown без keyup; `pressKey` не видел `page`).
- Баланс по решению владельца: зачистка 110 → 30 за пирата (замер 796 → 217, рядом с курьером 235), анархии убраны из заказчиков (на чарте их 0/64), описание док-компьютера приведено к механике.
- #13 закрыта решением владельца (срез принят до живого теста, возможен возврат). Открыты только #8 и #11 (дизайн, владелец).
- Мусор диагностики ссылок (`probe-head`, `probe-head2`, `probe-tag`) удалён.

## Что осталось сделать

- [ ] Живой тест игры владельцем → вердикт по срезу зачистки (оставить / крутить)
- [ ] Решение владельца по #8 (док-компьютер: оставить створ / полный автодок)
- [ ] Решение владельца по #11 (курьеры; возможно развяжется замером после #13)
- [ ] Перезамер частоты курьеров по доскам, когда дойдут руки (было 23% досок)

## Ключевые файлы

- `docs/CONTEXT.md` — журнал (вверху свежие строки), таблица открытых (#8, #11)
- `docs/CODE-REVIEW.md` — оглавление всех ревизий
- `src/logic/missions.js` — ставка зачистки (константа 30), фильтр заказчиков (gov !== 0), кламп сроков
- `src/logic/player.js` — `validateSave` (whitelist, вложенная память, дробные записи), описание `dock`
- `src/main.js` — `teardownScene` (единое снятие сцены), `boardFor` (ключ с баком/трюмом/активностью)
- `src/core/input.js` — счётчик отказов, тихие выходы, ребро нажатия
- `scripts/e2e.mjs` — `waitFor`, `pressKey` (полный цикл!), парковка 0.75 сферы
- `scripts/economy-sim.mjs` — честные дни (прыжок + док), допущения в шапке

## Контекст (может не быть в CONTEXT.md)

- Shell здесь pwsh: НЕТ `head`/`tail`/`grep`/`cat` — использовать `findstr`, `Get-Content`, `Remove-Item`. Вывод `npm test` перенаправлять в файл (`> out.txt 2>&1`), иначе съедает кодировку.
- `node --check` не ловит ошибки области видимости — только синтаксис.
- Юнит-тесты: 694, e2e: 91, сборка ~723 КБ. Всё зелёное на HEAD `07296c4`.
- Владелец сам не коммитит и не пушит — это делает агент, без лишних вопросов.
- Владелец общается на «ты», по-русски, архитектор; в чате без шифровок вида `file:123`, но в рабочих документах конкретика (файлы, строки) обязательна.
- Не создавать `.md` без явного разрешения (кроме регламентных HANDOFF/журнала).
- Сага про «рассинхронизацию ветки» (CRLF packed-refs, rmdir на D:) — непроверенное наблюдение окружения, не знание проекта. Git сейчас здоров.
- Странное поведение прошлого исполнителя: ложное обвинение ревью (исправлено им же через 3 часа с хронологией), коммит служебного файла в дерево (убран), пустые метки-коммиты с revert. Следить за гигиеной коммитов.

## Команды для проверки

```bash
npm test
npm run build
node scripts/economy-sim.mjs 800
gh run list --repo AlexanderKuzikov/Elite-Deep --limit 3
```

## Следующий шаг

Прочитать `docs/CONTEXT.md` (журнал сверху + таблица открытых #8/#11), убедиться `git status -sb` чист и `git log --oneline -3` начинается с `07296c4`, затем ждать владельца: живой тест и решения по дизайну.
